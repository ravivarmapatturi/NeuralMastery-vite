import { useState } from 'react';
import { useVizTokens } from '../../theme/vizTokens';
import { VisualizationContainer } from '../primitives';
import { getConceptColor } from './diagramSystem';
const STEPS = [{ title: 'Decision', detail: 'What action changes when this prediction arrives?', color: 'query' as const }, { title: 'Output geometry', detail: 'Choose label, box, mask, point, or track before collecting labels.', color: 'key' as const }, { title: 'Allowed variation', detail: 'Specify which changes should not alter the answer: lighting, crop, viewpoint, or background.', color: 'value' as const }, { title: 'Failure cost', detail: 'Set thresholds, review paths, and latency requirements from the consequence of an error.', color: 'attention' as const }];
export default function VisionDataContractDiagram() { const t = useVizTokens(); const [active, setActive] = useState(0); return <VisualizationContainer footer={STEPS[active].detail}><div style={{ fontSize: 13, fontWeight: 700, marginBottom: 10 }}>Specify the data contract before choosing a backbone</div><svg width="100%" viewBox="0 0 600 118" style={{ display: 'block' }}>{STEPS.map((s, i) => { const x = 16 + i * 148; const c = getConceptColor(t, s.color); const on = active === i; return <g key={s.title} onClick={() => setActive(i)} onMouseEnter={() => setActive(i)} style={{ cursor: 'pointer' }}>{i > 0 && <path d={`M${x - 18} 51 H${x - 4}`} stroke={t.textMuted} strokeWidth="1.5" markerEnd="url(#vdc-arrow)"/>}<rect x={x} y="26" width="126" height="52" rx="8" fill={on ? `${c}30` : t.surfaceAlt} stroke={c} strokeWidth={on ? 2.5 : 1.5}/><text x={x + 63} y="57" textAnchor="middle" fontSize="11" fontWeight="700" fill={c}>{s.title}</text></g>; })}<defs><marker id="vdc-arrow" markerWidth="7" markerHeight="7" refX="6" refY="3.5" orient="auto"><path d="M0 0 L7 3.5 L0 7 Z" fill={t.textMuted}/></marker></defs><text x="300" y="103" textAnchor="middle" fontSize="10" fill={t.textMuted}>Hover a decision to see the requirement it adds.</text></svg></VisualizationContainer>; }
