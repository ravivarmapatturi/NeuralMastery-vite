---
title: "Practice: Perplexity From Cross-Entropy Loss"
description: "Implement perplexity computation, the real, standard way a language model's cross-entropy loss is reported as an interpretable metric."
difficulty: "easy"
topic: "Transformers & LLMs"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Perplexity From Cross-Entropy Loss

**Track**: Transformers & LLMs | **Points**: 40 XP

## Overview

Implement perplexity computation, the real, standard way a language model's cross-entropy loss is reported as an interpretable metric.

## Task

Implement `perplexity(cross_entropy_loss)`: return `exp(cross_entropy_loss)`.

<PracticePlayground problemId="llm-internals-prob-41" />
