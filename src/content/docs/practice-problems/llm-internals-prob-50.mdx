---
title: "Practice: INT8 Quantization Scale Computation"
description: "Implement symmetric INT8 quantization scale computation, the real first step of quantizing a weight tensor's floating-point values into the int8 range."
difficulty: "medium"
topic: "Transformers & LLMs"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# INT8 Quantization Scale Computation

**Track**: Transformers & LLMs | **Points**: 40 XP

## Overview

Implement symmetric INT8 quantization scale computation, the real first step of quantizing a weight tensor's floating-point values into the int8 range.

## Task

Implement `int8_quant_scale(max_abs_value)`: return `max_abs_value / 127.0` (symmetric quantization maps `[-max_abs_value, max_abs_value]` to `[-127, 127]`).

<PracticePlayground problemId="llm-internals-prob-50" />
