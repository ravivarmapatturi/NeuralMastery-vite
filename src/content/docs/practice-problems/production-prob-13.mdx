---
title: "Practice: Token Streaming Buffer Flush Timing"
description: "Implement streaming-buffer flush timing, the real dual-trigger policy (size OR time) balancing network efficiency against perceived streaming responsiveness."
difficulty: "medium"
topic: "Agent Performance & Production"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Token Streaming Buffer Flush Timing

**Track**: Agent Performance & Production | **Points**: 40 XP

## Overview

Implement streaming-buffer flush timing, the real dual-trigger policy (size OR time) balancing network efficiency against perceived streaming responsiveness.

## Task

Implement `should_flush_buffer(buffer_size, max_buffer_size, time_since_last_flush_ms, max_flush_interval_ms)`: return `True` if EITHER `buffer_size >= max_buffer_size` OR `time_since_last_flush_ms >= max_flush_interval_ms`.

<PracticePlayground problemId="production-prob-13" />
