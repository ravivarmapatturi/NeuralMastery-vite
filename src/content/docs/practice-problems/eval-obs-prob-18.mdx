---
title: "Practice: Compute Eval Confidence Interval (Wilson Score)"
description: "Implement the Wilson score confidence interval, a real, more statistically sound alternative to a naive normal-approximation interval for reporting uncertainty on a small-sample eval pass rate."
difficulty: "hard"
topic: "Agent Evaluation & Observability"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Compute Eval Confidence Interval (Wilson Score)

**Track**: Agent Evaluation & Observability | **Points**: 40 XP

## Overview

Implement the Wilson score confidence interval, a real, more statistically sound alternative to a naive normal-approximation interval for reporting uncertainty on a small-sample eval pass rate.

## Task

Implement `wilson_interval(successes, total, z)` using the real Wilson score formula: `p = successes/total`; `denom = 1 + z**2/total`; `center = (p + z**2/(2*total)) / denom`; `margin = (z * sqrt(p*(1-p)/total + z**2/(4*total**2))) / denom`. Return `(center - margin, center + margin)`.

<PracticePlayground problemId="eval-obs-prob-18" />
