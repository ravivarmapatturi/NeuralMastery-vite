---
title: "Practice: Can Place Flowers"
description: "Master #653. Dynamic Programming & Greedy Module 1: Problem 3 in the Dynamic Programming & Greedy track of the 1,000+ problem Neural Mastery AI Engineering curriculum."
difficulty: "easy"
topic: "Dynamic Programming & Greedy"
---

import PracticePlayground from '../../../components/content/PracticePlayground';
import { Solution } from '../../../components/content/ExpandableDepth';

# #653. Dynamic Programming & Greedy Module 1: Problem 3

**Track**: Dynamic Programming & Greedy | **Rank**: #653 of 1072 | **Points**

## Overview

Determine if n flowers can be planted in empty plots without violating the adjacency rule using a greedy scan.

## Task

Implement `can_place_flowers(flowerbed, n)`: iterate through plots. Whenever current plot is 0 and both neighbors (treating out-of-bounds as 0) are 0, plant a flower greedily and decrement n.

<PracticePlayground problemId="dp-greedy-prob-3" />

<Solution>

```python
def can_place_flowers(flowerbed, n):
    count = 0
    beds = list(flowerbed)
    length = len(beds)
    for i in range(length):
        if beds[i] == 0:
            left = (i == 0 or beds[i - 1] == 0)
            right = (i == length - 1 or beds[i + 1] == 0)
            if left and right:
                beds[i] = 1
                count += 1
                if count >= n:
                    return True
    return count >= n
```

Greedily plant in any valid plot (0 with 0 neighbors). Early placement maximizes available future slots without reducing capacity.

</Solution>
