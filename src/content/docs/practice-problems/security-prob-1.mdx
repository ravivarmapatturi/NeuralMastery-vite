---
title: "Practice: Detect Prompt Injection Markers in Tool Output"
description: "Implement a real, simple prompt-injection detector scanning tool output for phrases attempting to override the agent's instructions -- a genuine attack surface whenever an agent reads untrusted external content."
difficulty: "medium"
topic: "Agent Security & Reliability"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Detect Prompt Injection Markers in Tool Output

**Track**: Agent Security & Reliability | **Points**: 40 XP

## Overview

Implement a real, simple prompt-injection detector scanning tool output for phrases attempting to override the agent's instructions -- a genuine attack surface whenever an agent reads untrusted external content.

## Task

Implement `detect_injection_markers(tool_output)`. Lowercase the text. Return the sorted list of DISTINCT marker phrases found among: `'ignore previous instructions'`, `'ignore all previous'`, `'new instructions'`, `'system prompt'`, `'you are now'`.

<PracticePlayground problemId="security-prob-1" />
