---
title: "Practice: Longest Increasing Subsequence"
description: "Master Longest Increasing Subsequence in the Dynamic Programming & Greedy track of the 1,000+ problem Neural Mastery AI Engineering curriculum."
difficulty: "medium"
topic: "Dynamic Programming & Greedy"
---

import PracticePlayground from '../../../components/content/PracticePlayground';
import { Solution } from '../../../components/content/ExpandableDepth';

# #663. Dynamic Programming & Greedy Module 3: Problem 13

**Track**: Dynamic Programming & Greedy | **Rank**: #663 of 1072 | **Points**

## Overview

Determine the length of the longest strictly increasing subsequence in O(n log n) time using patience sorting.

## Task

Implement `longest_increasing_subsequence(nums)`: maintain tails array where tails[i] is the smallest tail of all increasing subsequences of length i+1. Use binary search (bisect_left) to update tails.

<PracticePlayground problemId="dp-greedy-prob-13" />

<Solution>

```python
def longest_increasing_subsequence(nums):
    import bisect
    tails = []
    for x in nums:
        idx = bisect.bisect_left(tails, x)
        if idx == len(tails):
            tails.append(x)
        else:
            tails[idx] = x
    return len(tails)
```

For each number, find its insertion point in tails via binary search. Replace the first element `>= x` or append if x is strictly greater than all tails.

</Solution>
