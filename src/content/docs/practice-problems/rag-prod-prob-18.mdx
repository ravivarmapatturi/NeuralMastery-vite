---
title: "Practice: Answer Relevance via Embedding Cosine"
description: "Implement answer-relevance scoring via embedding cosine similarity, the standard automated proxy for 'does this answer actually address the question.'"
difficulty: "easy"
topic: "RAG Production Engineering"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Answer Relevance via Embedding Cosine

**Track**: RAG Production Engineering | **Points**: 40 XP

## Overview

Implement answer-relevance scoring via embedding cosine similarity, the standard automated proxy for 'does this answer actually address the question.'

## Task

Implement `answer_relevance(answer_embedding, question_embedding)`: return the cosine similarity between the two vectors.

<PracticePlayground problemId="rag-prod-prob-18" />
