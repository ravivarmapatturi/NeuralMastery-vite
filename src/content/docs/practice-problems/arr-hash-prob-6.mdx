---
title: "Practice: #536. Valid Palindrome"
description: "Master #536. Valid Palindrome in the Arrays / Hashing / Two Pointers track of the 1,000+ problem Neural Mastery AI Engineering curriculum."
difficulty: "easy"
topic: "Arrays / Hashing / Two Pointers"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# #536. Valid Palindrome

**Track**: Arrays / Hashing / Two Pointers | **Rank**: #536 of 1072 | **Points**: 40 XP

## Overview

Implement `is_palindrome(s)` using the classic two-pointer inward-convergence technique -- scanning from left and right while filtering out punctuation and case differences.

## Task

Implement the function `is_palindrome(s)`: return `True` if `s` is a palindrome (reading the same forwards and backwards after converting all uppercase letters to lowercase and removing all non-alphanumeric characters), or `False` otherwise.

<PracticePlayground problemId="arr-hash-prob-6" />
