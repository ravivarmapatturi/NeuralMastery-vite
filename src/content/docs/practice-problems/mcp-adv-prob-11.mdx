---
title: "Practice: Truncate Oversized Tool Output"
description: "Implement tool-output truncation with a clear indicator, protecting an agent's context window from being consumed by one oversized tool result."
difficulty: "easy"
topic: "MCP Advanced & Agent Integration"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Truncate Oversized Tool Output

**Track**: MCP Advanced & Agent Integration | **Points**: 40 XP

## Overview

Implement tool-output truncation with a clear indicator, protecting an agent's context window from being consumed by one oversized tool result.

## Task

Implement `truncate_tool_output(output, max_chars)`. If `len(output) <= max_chars`, return it unchanged. Otherwise return the first `max_chars` characters followed by the literal string `'... [truncated N more chars]'` where N is the number of characters that were cut.

<PracticePlayground problemId="mcp-adv-prob-11" />
