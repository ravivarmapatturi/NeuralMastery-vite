---
title: "Practice: Speculative Decoding Acceptance Rate"
description: "Implement the real speedup estimate for speculative decoding, a production LLM-serving optimization where a small draft model proposes tokens a larger model verifies in parallel."
difficulty: "medium"
topic: "Agent Performance & Production"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Speculative Decoding Acceptance Rate

**Track**: Agent Performance & Production | **Points**: 40 XP

## Overview

Implement the real speedup estimate for speculative decoding, a production LLM-serving optimization where a small draft model proposes tokens a larger model verifies in parallel.

## Task

Implement `spec_decode_speedup(acceptance_rate, draft_tokens_per_step)`: return the EXPECTED number of tokens accepted per verification step: `sum(acceptance_rate**i for i in range(1, draft_tokens_per_step+1)) + 1` -- the `+1` accounts for the guaranteed token the main model always produces even if all drafts are rejected.

<PracticePlayground problemId="production-prob-18" />
