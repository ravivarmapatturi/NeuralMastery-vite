---
title: "Practice: Consistent-Hashing Shard Routing"
description: "Implement deterministic shard routing for a distributed vector index -- every write and query must independently agree on which shard owns a given vector."
difficulty: "medium"
topic: "Vector Search & Index Optimization"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Consistent-Hashing Shard Routing

**Track**: Vector Search & Index Optimization | **Points**: 40 XP

## Overview

Implement deterministic shard routing for a distributed vector index -- every write and query must independently agree on which shard owns a given vector.

## Task

Implement `route_shard(vector_id, num_shards)`. Compute a stable hash of `vector_id` using Python's `hashlib.md5` on its UTF-8 bytes, interpret the first 8 bytes of the digest as an unsigned big-endian integer, and return `that_integer % num_shards`.

<PracticePlayground problemId="vec-search-prob-20" />
