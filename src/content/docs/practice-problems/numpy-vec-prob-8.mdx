---
title: "Practice: Vectorized Batch Matrix Multiplication with Transpose"
description: "Master #748. NumPy & Vectorized Computation Module 2: Problem 8 in the NumPy & Vectorized Computation track of the 1,000+ problem Neural Mastery AI Engineering curriculum."
difficulty: "easy"
topic: "NumPy & Vectorized Computation"
---

import PracticePlayground from '../../../components/content/PracticePlayground';
import { Solution } from '../../../components/content/ExpandableDepth';

# #748. Vectorized Batch Matrix Multiplication with Transpose

**Track**: NumPy & Vectorized Computation | **Rank**: #748 of 1072 | **Points**: 40 XP

## Overview

Compute batch tensor contractions involving transpose operations on the second operand.

## Task

Implement `batch_matmul_transpose(batch_A, batch_B)`: for each batch index `b`, compute matrix product `A @ B^T` where `A` has shape `(M, K)` and `B` has shape `(N, K)`. The resulting matrix has shape `(M, N)`. Round all entries to 4 decimals.

<PracticePlayground problemId="numpy-vec-prob-8" />

<Solution>

```python
def batch_matmul_transpose(batch_A, batch_B):
    res = []
    for A, B in zip(batch_A, batch_B):
        M, N = len(A), len(B)
        K = len(A[0]) if M > 0 else 0
        mat = [[round(float(sum(A[i][k] * B[j][k] for k in range(K))), 4) for j in range(N)] for i in range(M)]
        res.append(mat)
    return res
```

Calculates row-by-row dot products between A and B to yield A @ B^T.

</Solution>
