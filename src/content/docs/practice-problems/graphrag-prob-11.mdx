---
title: "Practice: Detect Conflicting Facts for the Same Subject/Predicate"
description: "Implement knowledge-conflict detection, catching cases where a graph contains two DIFFERENT objects for the same (subject, predicate) pair -- a real, common signal of extraction error or genuinely contradictory sources."
difficulty: "medium"
topic: "Knowledge Graph & GraphRAG"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Detect Conflicting Facts for the Same Subject/Predicate

**Track**: Knowledge Graph & GraphRAG | **Points**: 40 XP

## Overview

Implement knowledge-conflict detection, catching cases where a graph contains two DIFFERENT objects for the same (subject, predicate) pair -- a real, common signal of extraction error or genuinely contradictory sources.

## Task

Implement `find_conflicting_facts(triples)`. Group triples by `(subject, predicate)`. Return the sorted list of `f'{subject}:{predicate}'` strings for every group that has MORE THAN ONE distinct `object` value.

<PracticePlayground problemId="graphrag-prob-11" />
