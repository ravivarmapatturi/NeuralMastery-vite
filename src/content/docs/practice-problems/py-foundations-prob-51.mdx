---
title: "Practice: Closed Interval List Intersections"
sidebar_position: 51
description: "Compute pairwise intersection overlaps between two disjoint sorted interval streams in linear time."
difficulty: hard
topic: python-fundamentals
---

import PracticePlayground from '../../../components/content/PracticePlayground';
import { Solution } from '../../../components/content/ExpandableDepth';

# Closed Interval List Intersections

**Rank #51** · Difficulty: **Hard** · Stage: **Stage 1: AI Foundations & Python Programming** · Category: **Python & Fundamentals**

## Overview

Compute pairwise intersection overlaps between two disjoint sorted interval streams in linear time.

## Task

Implement `interval_intersections(first_list, second_list)`: given two lists of closed intervals `[start, end]` (each list internally pairwise disjoint and sorted by start time), compute and return all overlapping interval intersections.

<PracticePlayground problemId="py-foundations-prob-51" />

<Solution>

```python
def interval_intersections(first_list, second_list):
    i, j = 0, 0
    intersections = []
    while i < len(first_list) and j < len(second_list):
        start = max(first_list[i][0], second_list[j][0])
        end = min(first_list[i][1], second_list[j][1])
        if start <= end:
            intersections.append([start, end])
        if first_list[i][1] < second_list[j][1]:
            i += 1
        else:
            j += 1
    return intersections
```

Comparing boundaries with two pointers and advancing the earlier-ending interval guarantees full linear O(M + N) coverage.

</Solution>
