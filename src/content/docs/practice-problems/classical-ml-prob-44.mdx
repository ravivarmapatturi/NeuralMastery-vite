---
title: "Practice: Clustering Module 44: Problem 284"
sidebar_position: 284
description: "Curriculum Problem 284 (Stage 5: Classical Machine Learning & Optimization): Master Clustering / recommenders."
difficulty: medium
topic: classical-ml
---

import PracticePlayground from '../../../components/content/PracticePlayground';
import { Solution } from '../../../components/content/ExpandableDepth';

# Clustering Module 44: Problem 284

**Rank #284** · Difficulty: **Medium** · Stage: **Stage 5: Classical Machine Learning & Optimization** · Category: **Classical ML**

## Overview

Predict item ratings using the item-item differential Slope One recommendation scheme.

## Task

Implement `slope_one_predict(ratings, dev_matrix, count_matrix, target_item)`: for user ratings, prediction is `sum((r_ui + dev(target, i)) * count(target, i)) / sum(count(target, i))` over rated items `i != target_item`. Return 0.0 if no co-rated items exist. Round float to 4 decimal places.

<PracticePlayground problemId="classical-ml-prob-44" />

<Solution>

```python
def slope_one_predict(ratings, dev_matrix, count_matrix, target_item):
    num = 0.0
    denom = 0
    for item_i, r_ui in ratings.items():
        if item_i != target_item:
            dev = dev_matrix.get(target_item, {}).get(item_i, 0.0)
            cnt = count_matrix.get(target_item, {}).get(item_i, 0)
            if cnt > 0:
                num += (r_ui + dev) * cnt
                denom += cnt
    pred = num / denom if denom > 0 else 0.0
    return round(float(pred), 4)
```

Slope One tracks pairwise average rating differences between items, providing an intuitive, parameter-free collaborative filtering baseline.

</Solution>
