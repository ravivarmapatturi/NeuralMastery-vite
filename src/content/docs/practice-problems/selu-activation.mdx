---
title: "Practice: SELU (Scaled Exponential Linear Unit)"
sidebar_position: 77
description: "Implement the self-normalizing SELU activation function with exact scale (lambda) and alpha constants."
difficulty: medium
topic: deep-learning
---

import PracticePlayground from '../../../components/content/PracticePlayground';
import { Solution } from '../../../components/content/ExpandableDepth';

# SELU (Scaled Exponential Linear Unit)

**Difficulty: Medium** · Topic: Deep Learning

Implement the self-normalizing SELU activation function with exact scale (lambda) and alpha constants.

<PracticePlayground problemId="selu-activation" />

<Solution>

```python
import math

def selu(x, scale=1.0507009873554805, alpha=1.6732632423543772):
    return [scale * val if val > 0 else scale * alpha * (math.exp(val) - 1.0) for val in x]
```

SELU enables self-normalizing neural networks where activations converge toward zero mean and unit variance without explicit BatchNorm layers.

</Solution>
