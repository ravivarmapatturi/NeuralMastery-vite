---
title: "Practice: Porter Stemmer Step 1a Suffix Reduction"
description: "Implement Step 1a of the classic Porter Stemmer algorithm for plural and suffix normalization in information retrieval."
difficulty: "easy"
topic: "Deep Learning & Vision/NLP"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Porter Stemmer Step 1a Suffix Reduction

**Track**: Deep Learning & Vision/NLP | **Points**: 40 XP

## Overview

Implement Step 1a of the classic Porter Stemmer algorithm for plural and suffix normalization in information retrieval.

## Task

Implement `porter_stemmer_step1a(word)`. Apply the first matching rule in order: 1) if `word` ends in `'sses'`, replace with `'ss'`; 2) elif `word` ends in `'ies'`, replace with `'i'`; 3) elif `word` ends in `'ss'`, leave unchanged; 4) elif `word` ends in `'s'` and length is at least 3 and character before `'s'` is NOT `'s'`, remove `'s'`. Return the resulting stemmed string.

<PracticePlayground problemId="deep-learning-prob-43" />
