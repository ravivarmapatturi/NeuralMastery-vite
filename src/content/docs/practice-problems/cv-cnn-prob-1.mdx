---
title: "Practice: Conv2D Output Spatial Dimension Calculator"
description: "Master Conv2D Output Spatial Dimension Calculator in the Computer Vision & CNN Architectures track of the 1,000+ problem Neural Mastery AI Engineering curriculum."
difficulty: "easy"
topic: "Computer Vision & CNN Architectures"
---

import PracticePlayground from '../../../components/content/PracticePlayground';
import { Solution } from '../../../components/content/ExpandableDepth';

# #891. Conv2D Output Spatial Dimension Calculator

**Track**: Computer Vision & CNN Architectures | **Rank**: #891 of 1072 | **Points**: 40 XP

## Overview

Calculate exact output spatial feature map dimensions for convolutional layers across various padding, stride, and dilation configurations.

## Task

Implement `conv2d_output_shape(...)`: calculate the output spatial shape `[out_H, out_W]`. The effective kernel dimension with dilation `d` is `k_eff = k + (k - 1) * (d - 1)`. The output dimension is `floor((dim + 2 * pad - k_eff) / stride) + 1`. Return `[out_H, out_W]` as integers.

<PracticePlayground problemId="cv-cnn-prob-1" />

<Solution>

```python
def conv2d_output_shape(H: int, W: int, kH: int, kW: int, pad_h: int = 0, pad_w: int = 0, stride_h: int = 1, stride_w: int = 1, dilation_h: int = 1, dilation_w: int = 1) -> list[int]:
    eff_kH = kH + (kH - 1) * (dilation_h - 1)
    eff_kW = kW + (kW - 1) * (dilation_w - 1)
    out_H = math.floor((H + 2 * pad_h - eff_kH) / stride_h) + 1
    out_W = math.floor((W + 2 * pad_w - eff_kW) / stride_w) + 1
    return [out_H, out_W]
```

Accounting for dilation expands the effective kernel footprint, and applying stride downsamples the resulting feature map.

</Solution>
