---
title: "Practice: Label Smoothing Applied to Target Distribution"
description: "Implement label smoothing, a real regularization technique softening the hard one-hot training target to prevent overconfident LLM predictions."
difficulty: "medium"
topic: "Transformers & LLMs"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Label Smoothing Applied to Target Distribution

**Track**: Transformers & LLMs | **Points**: 40 XP

## Overview

Implement label smoothing, a real regularization technique softening the hard one-hot training target to prevent overconfident LLM predictions.

## Task

Implement `label_smoothing(target_idx, vocab_size, smoothing)`. The target distribution assigns `1 - smoothing + smoothing/vocab_size` to the true token, and `smoothing/vocab_size` to every other token. Return the full `vocab_size`-length distribution.

<PracticePlayground problemId="llm-internals-prob-57" />
