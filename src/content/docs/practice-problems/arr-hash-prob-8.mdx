---
title: "Practice: Two Sum II - Input Array Is Sorted"
description: "Master #538. Two Sum II - Input Array Is Sorted in the Arrays / Hashing / Two Pointers track of the 1,000+ problem Neural Mastery AI Engineering curriculum."
difficulty: "easy"
topic: "Arrays / Hashing / Two Pointers"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# #538. Two Sum II - Input Array Is Sorted

**Track**: Arrays / Hashing / Two Pointers | **Rank**: #538 of 1072 | **Points**: 40 XP

## Overview

Implement `two_sum_sorted(numbers, target)` for a sorted array using inward-marching two pointers to find the target pair without extra memory allocation.

## Task

Implement `two_sum_sorted(numbers, target)`: return the 1-indexed positions `[index1, index2]` of the two numbers that add up to `target`. Exactly one valid pair is guaranteed to exist.

<PracticePlayground problemId="arr-hash-prob-8" />
