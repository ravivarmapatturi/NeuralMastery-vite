---
title: "Practice: Denial-of-Wallet Cost Spike Detection"
description: "Implement denial-of-wallet cost-spike detection, a real financial guardrail catching a runaway agent (or an attack exploiting it) before an unbounded API bill accrues."
difficulty: "medium"
topic: "Agent Security & Reliability"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Denial-of-Wallet Cost Spike Detection

**Track**: Agent Security & Reliability | **Points**: 40 XP

## Overview

Implement denial-of-wallet cost-spike detection, a real financial guardrail catching a runaway agent (or an attack exploiting it) before an unbounded API bill accrues.

## Task

Implement `is_cost_spike(current_hour_cost, rolling_avg_cost, spike_multiplier)`. If `rolling_avg_cost` is `0`, return `True` if `current_hour_cost > 0` (any cost against a zero baseline is a spike). Otherwise return `True` if `current_hour_cost > rolling_avg_cost * spike_multiplier`.

<PracticePlayground problemId="security-prob-28" />
