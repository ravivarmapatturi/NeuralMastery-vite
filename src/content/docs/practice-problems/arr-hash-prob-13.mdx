---
title: "Practice: #543. Arrays / Hashing / Two Pointers Module 3: Problem 13"
description: "Master #543. Arrays / Hashing / Two Pointers Module 3: Problem 13 in the Arrays / Hashing / Two Pointers track of the 1,000+ problem Neural Mastery AI Engineering curriculum."
difficulty: "medium"
topic: "Arrays / Hashing / Two Pointers"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# #543. Arrays / Hashing / Two Pointers Module 3: Problem 13

**Track**: Arrays / Hashing / Two Pointers | **Rank**: #543 of 1072 | **Points**: 60 XP

## Overview

Implement `longest_consecutive(nums)` via the O(n) hash-set solution -- the classic case where sorting FEELS natural but is the wrong complexity, and a hash set achieves true O(n).

## Task

Implement the function `longest_consecutive(nums)`: return the length of the longest run of consecutive integers present in `nums` (in any order in the input). Must run in O(n) time (no sorting).

<PracticePlayground problemId="arr-hash-prob-13" />
