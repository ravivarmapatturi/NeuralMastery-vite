---
title: "Practice: Shadow Traffic Comparison Divergence Rate"
description: "Implement shadow-traffic divergence rate calculation, the real metric measuring how often a candidate model version's output actually differs from production when tested on mirrored live traffic (with no user-visible effect)."
difficulty: "medium"
topic: "Agent Performance & Production"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Shadow Traffic Comparison Divergence Rate

**Track**: Agent Performance & Production | **Points**: 40 XP

## Overview

Implement shadow-traffic divergence rate calculation, the real metric measuring how often a candidate model version's output actually differs from production when tested on mirrored live traffic (with no user-visible effect).

## Task

Implement `divergence_rate(matching_responses, total_shadow_requests)`: return `1 - (matching_responses / total_shadow_requests)`. Return `0.0` if `total_shadow_requests` is 0.

<PracticePlayground problemId="production-prob-29" />
