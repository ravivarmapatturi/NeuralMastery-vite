---
title: "Practice: Harris Corner Detector Response Score"
description: "Compute the Harris corner response score R = det(M) - k * (trace(M))^2 for local image feature detection."
difficulty: "easy"
topic: "Deep Learning & Vision/NLP"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Harris Corner Detector Response Score

**Track**: Deep Learning & Vision/NLP | **Points**: 40 XP

## Overview

Compute the Harris corner response score R = det(M) - k * (trace(M))^2 for local image feature detection.

## Task

Implement `harris_corner_response(Ixx, Iyy, Ixy, k)`. For structure tensor matrix `M = [[Ixx, Ixy], [Ixy, Iyy]]`, compute `det_M = Ixx * Iyy - Ixy ** 2` and `trace_M = Ixx + Iyy`. The corner response is `R = det_M - k * (trace_M ** 2)`. Round result to 4 decimal places.

<PracticePlayground problemId="deep-learning-prob-31" />
