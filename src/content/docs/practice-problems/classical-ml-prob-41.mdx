---
title: "Practice: Gradient Boosting Pseudo-Residuals"
sidebar_position: 281
description: "Curriculum Problem 281 (Stage 5: Classical Machine Learning & Optimization): Master Classical ML."
difficulty: medium
topic: classical-ml
---

import PracticePlayground from '../../../components/content/PracticePlayground';
import { Solution } from '../../../components/content/ExpandableDepth';

# Classical ML Module 41: Problem 281

**Rank #281** · Difficulty: **Medium** · Stage: **Stage 5: Classical Machine Learning & Optimization** · Category: **Classical ML**

## Overview

Compute negative gradient pseudo-residuals for regression and binary classification gradient boosting.

## Task

Implement `gbm_pseudo_residuals(y_true, raw_predictions, loss_type)`: for `'regression'` (squared error), residual is `y - raw`. For `'classification'` (binary log loss with y in {0, 1}), residual is `y - sigmoid(raw)`. Return list of residuals rounded to 4 decimal places.

<PracticePlayground problemId="classical-ml-prob-41" />

<Solution>

```python
import math

def gbm_pseudo_residuals(y_true, raw_predictions, loss_type):
    res = []
    for yt, raw in zip(y_true, raw_predictions):
        if loss_type == 'regression':
            r = yt - raw
        else:
            p = 1.0 / (1.0 + math.exp(-raw)) if raw >= 0 else math.exp(raw) / (1.0 + math.exp(raw))
            r = yt - p
        res.append(round(float(r), 4))
    return res
```

Gradient boosting fits subsequent decision trees to the negative gradient of the loss function, generalising gradient descent to function space.

</Solution>
