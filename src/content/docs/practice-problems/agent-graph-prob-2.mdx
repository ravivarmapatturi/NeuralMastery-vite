---
title: "Practice: Conditional Edge Routing by State Predicate"
description: "Implement conditional-edge routing, the core mechanism letting an agent graph branch to different next nodes based on the current state (e.g. LangGraph's conditional edges)."
difficulty: "easy"
topic: "Agent Graph Engineering"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Conditional Edge Routing by State Predicate

**Track**: Agent Graph Engineering | **Points**: 40 XP

## Overview

Implement conditional-edge routing, the core mechanism letting an agent graph branch to different next nodes based on the current state (e.g. LangGraph's conditional edges).

## Task

Implement `route_conditional_edge(state, routes)`. `routes` is an ordered list of `{"key": str, "equals": value, "target": str}`. Return the `target` of the FIRST route where `state.get(key) == equals`. If none match, return `'__default__'`.

<PracticePlayground problemId="agent-graph-prob-2" />
