---
title: "Practice: Valid Parentheses"
description: "Master Valid Parentheses in the Linked Lists / Stacks / Queues track of the 1,000+ problem Neural Mastery AI Engineering curriculum."
difficulty: "easy"
topic: "Linked Lists / Stacks / Queues"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# #562. Linked Lists / Stacks / Queues Module 1: Problem 2

**Track**: Linked Lists / Stacks / Queues | **Rank**: #562 of 1072 | **Points**: 40 XP

## Overview

Implement `is_valid_parentheses(s)`, the canonical "does this need a stack?" tell -- correct nesting is exactly what a stack (last-opened, first-closed) is built to verify.

## Task

Implement the function `is_valid_parentheses(s)`: `s` contains only `(`, `)`, `{`, `}`, `[`, `]`. Return `True` if every bracket is properly matched and correctly nested.

<PracticePlayground problemId="ll-queue-prob-2" />
