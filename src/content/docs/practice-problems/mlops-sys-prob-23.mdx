---
title: "Practice: Pairwise Feature Correlation (Pearson)"
description: "Implement Pearson correlation, the real statistic behind a feature-correlation matrix used to detect redundant/collinear features before training."
difficulty: "medium"
topic: "MLOps & Data Systems"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Pairwise Feature Correlation (Pearson)

**Track**: MLOps & Data Systems | **Points**: 40 XP

## Overview

Implement Pearson correlation, the real statistic behind a feature-correlation matrix used to detect redundant/collinear features before training.

## Task

Implement `pearson_correlation(x, y)`: return the Pearson correlation coefficient between the two equal-length lists using the standard formula `cov(x,y) / (std(x) * std(y))`, with population (not sample) standard deviation and covariance.

<PracticePlayground problemId="mlops-sys-prob-23" />
