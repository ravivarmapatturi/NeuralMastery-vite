---
title: "Practice: One PageRank Iteration Step"
description: "Implement a single real PageRank power-iteration step, the algorithm behind ranking the most 'important' entities in a large knowledge graph by link structure."
difficulty: "hard"
topic: "Knowledge Graph & GraphRAG"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# One PageRank Iteration Step

**Track**: Knowledge Graph & GraphRAG | **Points**: 40 XP

## Overview

Implement a single real PageRank power-iteration step, the algorithm behind ranking the most 'important' entities in a large knowledge graph by link structure.

## Task

Implement `pagerank_step(graph, scores, damping)`. For each node `n`, its new score is `(1-damping)/N + damping * sum(scores[m]/len(graph[m]) for m in graph if n in graph[m])`, where `N = len(graph)`. A node with NO outgoing edges (`len(graph[m])==0`) contributes nothing to any node's new score (avoid dividing by zero). Return the dict of new scores.

<PracticePlayground problemId="graphrag-prob-13" />
