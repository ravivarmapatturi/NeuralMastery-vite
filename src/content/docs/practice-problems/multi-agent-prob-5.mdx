---
title: "Practice: Broadcast Fanout Excluding Sender"
description: "Implement broadcast fanout, the basic real message-distribution primitive in any multi-agent communication layer."
difficulty: "easy"
topic: "Multi-Agent Systems"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Broadcast Fanout Excluding Sender

**Track**: Multi-Agent Systems | **Points**: 40 XP

## Overview

Implement broadcast fanout, the basic real message-distribution primitive in any multi-agent communication layer.

## Task

Implement `broadcast_fanout(all_agents, sender)`: return `all_agents` with `sender` excluded (preserve original order, remove ALL occurrences of `sender` if duplicated).

<PracticePlayground problemId="multi-agent-prob-5" />
