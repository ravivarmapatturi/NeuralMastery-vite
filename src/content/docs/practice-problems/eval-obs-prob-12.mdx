---
title: "Practice: Statistical Significance of an Eval Score Delta"
description: "Implement statistical-significance testing for an eval score comparison, the real gate before claiming 'model B genuinely beats model A' rather than a difference explainable by eval-set sample noise."
difficulty: "hard"
topic: "Agent Evaluation & Observability"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Statistical Significance of an Eval Score Delta

**Track**: Agent Evaluation & Observability | **Points**: 40 XP

## Overview

Implement statistical-significance testing for an eval score comparison, the real gate before claiming 'model B genuinely beats model A' rather than a difference explainable by eval-set sample noise.

## Task

Implement `eval_delta_significant(pass_count_a, total_a, pass_count_b, total_b, z_threshold)` using the same two-proportion z-test as an A/B test: `p_a = pass_count_a/total_a`; `p_b = pass_count_b/total_b`; `p_pool = (pass_count_a+pass_count_b)/(total_a+total_b)`; `se = sqrt(p_pool*(1-p_pool)*(1/total_a + 1/total_b))`. Return `True` if `se > 0` and `abs(p_b-p_a)/se > z_threshold`.

<PracticePlayground problemId="eval-obs-prob-12" />
