---
title: "Practice: Log Sampling Rate for High-Volume Agent Traffic"
description: "Implement deterministic log sampling, a real technique for keeping observability cost manageable on high-volume production agent traffic without losing statistical visibility."
difficulty: "easy"
topic: "Agent Performance & Production"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Log Sampling Rate for High-Volume Agent Traffic

**Track**: Agent Performance & Production | **Points**: 40 XP

## Overview

Implement deterministic log sampling, a real technique for keeping observability cost manageable on high-volume production agent traffic without losing statistical visibility.

## Task

Implement `should_sample_log(request_id_hash, sample_rate)`: return `True` if `(request_id_hash % 10000) < (sample_rate * 10000)`.

<PracticePlayground problemId="production-prob-26" />
