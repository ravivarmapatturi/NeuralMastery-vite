---
title: "Practice: Exact Match (EM) Evaluation Metric"
description: "Compute the Exact Match (EM) evaluation metric for QA benchmarks with text normalization."
difficulty: "easy"
topic: "transformers-llms"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Exact Match (EM) Evaluation Metric

**Track**: transformers-llms | **Points**: 40 XP

## Overview

Compute the Exact Match (EM) evaluation metric for QA benchmarks with text normalization.

## Task

Implement `exact_match_score(predictions, references)`. Normalize both prediction and reference text: convert to lowercase, strip punctuation, and normalize whitespace (`' '.join(text.split())`). Return the fraction of matching pairs rounded to 4 decimal places. Return 0.0 if empty or length mismatch.

<PracticePlayground problemId="llm-internals-prob-40" />
