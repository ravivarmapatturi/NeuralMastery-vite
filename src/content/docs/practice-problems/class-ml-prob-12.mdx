---
title: "Practice: Logistic Regression: Forward Pass + Log Loss"
description: "Master Logistic Regression: Forward Pass + Log Loss in the Classical Machine Learning track of the 1,000+ problem Neural Mastery AI Engineering curriculum."
difficulty: "medium"
topic: "Classical Machine Learning"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# #812. Classical Machine Learning Module 3: Problem 12

**Track**: Classical Machine Learning | **Rank**: #812 of 1072 | **Points**: 60 XP

## Overview

Implement `logistic_regression_loss(X, y, weights, bias)`, logistic regression's full forward pass and cost function -- the actual thing being minimized during training, distinct from just the sigmoid function itself.

## Task

Implement the function `logistic_regression_loss(X, y, weights, bias)`: for every row, compute z = w.x + b, squash it with sigmoid, then average the binary cross-entropy loss -(y*log(p) + (1-y)*log(1-p)) across all rows. Clip p to [1e-15, 1-1e-15] before taking a log.

<PracticePlayground problemId="class-ml-prob-12" />
