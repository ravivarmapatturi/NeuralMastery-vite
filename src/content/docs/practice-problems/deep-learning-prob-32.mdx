---
title: "Practice: Color Jitter Brightness Adjustment"
description: "Scale image intensities by a multiplicative jitter factor and clamp to valid normalized range [0.0, 1.0]."
difficulty: "easy"
topic: "Deep Learning & Vision/NLP"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Color Jitter Brightness Adjustment

**Track**: Deep Learning & Vision/NLP | **Points**: 40 XP

## Overview

Scale image intensities by a multiplicative jitter factor and clamp to valid normalized range [0.0, 1.0].

## Task

Implement `color_jitter_brightness(image, factor)`. Multiply each pixel intensity in the 2D image matrix by `factor` and clamp the result to `[0.0, 1.0]`: `clamped = max(0.0, min(1.0, pixel * factor))`. Round each cell to 4 decimal places.

<PracticePlayground problemId="deep-learning-prob-32" />
