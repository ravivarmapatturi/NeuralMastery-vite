---
title: "Practice: Non-Maximum Suppression (NMS)"
description: "Master #902. Computer Vision & CNN Architectures Module 3: Problem 12 in the Computer Vision & CNN Architectures track of the 1,000+ problem Neural Mastery AI Engineering curriculum."
difficulty: "medium"
topic: "Computer Vision & CNN Architectures"
---

import PracticePlayground from '../../../components/content/PracticePlayground';
import { Solution } from '../../../components/content/ExpandableDepth';

# #902. Non-Maximum Suppression (NMS)

**Track**: Computer Vision & CNN Architectures | **Rank**: #902 of 1072 | **Points**: 60 XP

## Overview

Eliminate redundant duplicate bounding box predictions in object detection pipelines using greedy Non-Maximum Suppression.

## Task

Implement `non_max_suppression(boxes, scores, iou_threshold=0.5)`: sort candidate box indices by descending confidence `scores`. Greedily select the highest-scoring box, add its index to `keep`, and suppress (discard) any remaining boxes whose IoU with it exceeds `iou_threshold`. Repeat until all candidates are processed. Return the list of kept indices.

<PracticePlayground problemId="cv-cnn-prob-12" />

<Solution>

```python
def non_max_suppression(boxes: list[list[float]], scores: list[float], iou_threshold: float = 0.5) -> list[int]:
    indices = sorted(range(len(scores)), key=lambda i: scores[i], reverse=True)
    keep = []

    def get_iou(b1, b2):
        xA = max(b1[0], b2[0]); yA = max(b1[1], b2[1])
        xB = min(b1[2], b2[2]); yB = min(b1[3], b2[3])
        inter = max(0.0, xB - xA) * max(0.0, yB - yA)
        a1 = (b1[2] - b1[0]) * (b1[3] - b1[1])
        a2 = (b2[2] - b2[0]) * (b2[3] - b2[1])
        u = a1 + a2 - inter
        return inter / u if u > 0.0 else 0.0

    while indices:
        current = indices.pop(0)
        keep.append(current)
        indices = [i for i in indices if get_iou(boxes[current], boxes[i]) <= iou_threshold]

    return keep
```

Iteratively keeping the maximum scoring candidate and filtering out proposals overlapping beyond the IoU threshold eliminates duplicate bounding boxes.

</Solution>
