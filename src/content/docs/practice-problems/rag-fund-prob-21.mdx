---
title: "Practice: #201. Document Freshness Invalidator"
description: "Master #201. Document Freshness Invalidator in the RAG Fundamentals track of the 1,000+ problem Neural Mastery AI Engineering curriculum."
difficulty: "medium"
topic: "RAG Fundamentals"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# #201. Document Freshness Invalidator

**Track**: RAG Fundamentals | **Rank**: #201 of 1072 | **Points**: 60 XP

## Overview

Maintain index currency by purging stale chunks and superseding outdated document versions.

## Task

Implement `document_freshness_invalidator(documents, current_timestamp, ttl_seconds)`. A document is expired if `current_timestamp - doc["timestamp"] > ttl_seconds`. Among non-expired documents sharing the same `doc_id`, the one with the highest `version` is active; any lower-version non-expired documents for that `doc_id` are superseded. Return `{"active": sorted(active_ids), "expired": sorted(expired_ids), "superseded": sorted(superseded_ids)}`.

<PracticePlayground problemId="rag-fund-prob-21" />
