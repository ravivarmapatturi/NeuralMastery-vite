---
title: "Practice: Filter Log Messages by Level Threshold"
description: "Implement MCP log-level filtering, respecting the client's requested minimum log verbosity for a server's `notifications/message` stream."
difficulty: "easy"
topic: "MCP Core Engineering"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Filter Log Messages by Level Threshold

**Track**: MCP Core Engineering | **Points**: 40 XP

## Overview

Implement MCP log-level filtering, respecting the client's requested minimum log verbosity for a server's `notifications/message` stream.

## Task

Implement `filter_logs_by_level(logs, min_level)`. `logs` is a list of `{"message": str, "level": str}`. Severity order (low to high) is `debug < info < warning < error`. Return the `message`s (in original order) of every log whose `level` is at or above `min_level`'s severity.

<PracticePlayground problemId="mcp-core-prob-15" />
