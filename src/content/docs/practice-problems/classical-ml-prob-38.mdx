---
title: "Practice: 1D Gaussian Mixture Model Maximization (M-Step)"
sidebar_position: 278
description: "Curriculum Problem 278 (Stage 5: Classical Machine Learning & Optimization): Master Clustering / recommenders."
difficulty: medium
topic: classical-ml
---

import PracticePlayground from '../../../components/content/PracticePlayground';
import { Solution } from '../../../components/content/ExpandableDepth';

# Clustering Module 38: Problem 278

**Rank #278** · Difficulty: **Medium** · Stage: **Stage 5: Classical Machine Learning & Optimization** · Category: **Classical ML**

## Overview

Update component weights, means, and variances in the GMM M-step.

## Task

Implement `gmm_m_step_1d(x, responsibilities)`: for each component k, compute effective count `N_k = sum(gamma_ik)`. Update `w_k = N_k / N`, `mu_k = sum(gamma_ik * x_i) / N_k`, and `var_k = sum(gamma_ik * (x_i - mu_k)**2) / N_k`. Return dict with keys `"weights"`, `"means"`, `"variances"`, rounded to 4 decimal places.

<PracticePlayground problemId="classical-ml-prob-38" />

<Solution>

```python
def gmm_m_step_1d(x, responsibilities):
    n = len(x)
    k = len(responsibilities[0])
    new_weights = []
    new_means = []
    new_variances = []
    for col in range(k):
        nk = sum(responsibilities[i][col] for i in range(n))
        w = nk / n
        mu = sum(responsibilities[i][col] * x[i] for i in range(n)) / nk if nk > 0 else 0.0
        var = sum(responsibilities[i][col] * ((x[i] - mu) ** 2) for i in range(n)) / nk if nk > 0 else 1.0
        new_weights.append(round(float(w), 4))
        new_means.append(round(float(mu), 4))
        new_variances.append(round(float(var), 4))
    return {"weights": new_weights, "means": new_means, "variances": new_variances}
```

The M-step analytically maximizes the expected complete-data log-likelihood with respect to Gaussian parameters given fixed posterior responsibilities.

</Solution>
