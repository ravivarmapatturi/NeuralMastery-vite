---
title: "Practice: Deduplicate Entity Mentions by Normalized Name"
description: "Implement entity-mention deduplication via name normalization, a real first step before building a knowledge graph from extracted text mentions."
difficulty: "easy"
topic: "Knowledge Graph & GraphRAG"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Deduplicate Entity Mentions by Normalized Name

**Track**: Knowledge Graph & GraphRAG | **Points**: 40 XP

## Overview

Implement entity-mention deduplication via name normalization, a real first step before building a knowledge graph from extracted text mentions.

## Task

Implement `dedup_entity_mentions(mentions)`. Normalize each mention by lowercasing and stripping leading/trailing whitespace. Return the sorted list of DISTINCT normalized names.

<PracticePlayground problemId="graphrag-prob-1" />
