---
title: "Practice: #825. Classical Machine Learning Module 5: Problem 25"
description: "Master #825. Classical Machine Learning Module 5: Problem 25 in the Classical Machine Learning track of the 1,000+ problem Neural Mastery AI Engineering curriculum."
difficulty: "hard"
topic: "Classical Machine Learning"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# #825. Classical Machine Learning Module 5: Problem 25

**Track**: Classical Machine Learning | **Rank**: #825 of 1072 | **Points**: 90 XP

## Overview

Implement `silhouette_score(X, labels)`, the standard way to numerically evaluate a clustering (like K-Means' output) WITHOUT ground-truth labels, by checking whether every point is closer to its own cluster than to the nearest other one.

## Task

Implement the function `silhouette_score(X, labels)`: for every point, compute its mean in-cluster distance a(i) and its mean distance to the nearest other cluster b(i), combine them into s(i) = (b-a)/max(a,b), and return the mean s(i) across all points. A singleton cluster gets s(i)=0.0 by convention. Raise `ValueError` if fewer than 2 clusters are present.

<PracticePlayground problemId="class-ml-prob-25" />
