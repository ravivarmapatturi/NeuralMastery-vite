---
title: "Practice: Vectorized Euclidean Distance Matrix via Gram Matrix"
sidebar_position: 232
description: "Curriculum Problem 232 (Stage 4: NumPy & Vectorized Data Processing): Master NumPy / vectorization."
difficulty: hard
topic: numpy-pandas
---

import PracticePlayground from '../../../components/content/PracticePlayground';
import { Solution } from '../../../components/content/ExpandableDepth';

# NumPy Module 52: Problem 232

**Rank #232** · Difficulty: **Hard** · Stage: **Stage 4: NumPy & Vectorized Data Processing** · Category: **NumPy & Pandas**

## Overview

Compute the complete pairwise Euclidean distance matrix between two feature sets.

## Task

Implement `pairwise_euclidean_distance(x, y)`: given matrices x (N x D) and y (M x D), compute the (N, M) matrix of L2 Euclidean distances `sqrt(sum((x_i - y_j)**2))`. Round each float to 4 decimal places.

<PracticePlayground problemId="numpy-pandas-prob-52" />

<Solution>

```python
import math

def pairwise_euclidean_distance(x, y):
    res = []
    for u in x:
        row = []
        for v in y:
            dist_sq = sum((a - b) ** 2 for a, b in zip(u, v))
            dist = math.sqrt(max(0.0, dist_sq))
            row.append(round(float(dist), 4))
        res.append(row)
    return res
```

Vectorized pairwise Euclidean distance uses the quadratic expansion ||x - y||^2 = ||x||^2 + ||y||^2 - 2(x . y) for high-performance vectorized evaluation.

</Solution>
