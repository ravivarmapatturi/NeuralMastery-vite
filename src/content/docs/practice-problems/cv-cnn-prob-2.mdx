---
title: "Practice: 2D Max Pooling Forward Pass"
description: "Master #892. Computer Vision & CNN Architectures Module 1: Problem 2 in the Computer Vision & CNN Architectures track of the 1,000+ problem Neural Mastery AI Engineering curriculum."
difficulty: "easy"
topic: "Computer Vision & CNN Architectures"
---

import PracticePlayground from '../../../components/content/PracticePlayground';
import { Solution } from '../../../components/content/ExpandableDepth';

# #892. 2D Max Pooling Forward Pass

**Track**: Computer Vision & CNN Architectures | **Rank**: #892 of 1072 | **Points**: 40 XP

## Overview

Downsample spatial representations and extract dominant features using 2D max pooling.

## Task

Implement `max_pool2d_forward(x, pool_size=2, stride=2)`: slide a square window of size `pool_size x pool_size` with step size `stride` over 2D input `x`. For each window position, take the maximum element. Round output floats to 4 decimal places.

<PracticePlayground problemId="cv-cnn-prob-2" />

<Solution>

```python
def max_pool2d_forward(x: list[list[float]], pool_size: int = 2, stride: int = 2) -> list[list[float]]:
    H = len(x)
    W = len(x[0])
    out_H = (H - pool_size) // stride + 1
    out_W = (W - pool_size) // stride + 1
    out = []
    for i in range(out_H):
        row = []
        for j in range(out_W):
            h_start = i * stride
            w_start = j * stride
            window_max = max(x[h_start + kh][w_start + kw] for kh in range(pool_size) for kw in range(pool_size))
            row.append(round(float(window_max), 4))
        out.append(row)
    return out
```

Max pooling extracts the maximum value within each sliding window, reducing spatial footprint while retaining prominent activations.

</Solution>
