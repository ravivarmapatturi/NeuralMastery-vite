---
title: "Practice: RoIAlign Bilinear Sampling Point Coordinates"
description: "Calculate continuous sub-pixel sampling coordinates for RoIAlign without spatial quantization artifacts."
difficulty: "medium"
topic: "Deep Learning & Vision/NLP"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# RoIAlign Bilinear Sampling Point Coordinates

**Track**: Deep Learning & Vision/NLP | **Points**: 40 XP

## Overview

Calculate continuous sub-pixel sampling coordinates for RoIAlign without spatial quantization artifacts.

## Task

Implement `roi_align_sample_points(roi, bin_r, bin_c, pooled_h, pooled_w, sampling_ratio)`. `roi` is `[x1, y1, x2, y2]`. The bin width is `bin_w = (x2 - x1) / pooled_w` and height is `bin_h = (y2 - y1) / pooled_h`. The bin top-left is `start_x = x1 + bin_c * bin_w` and `start_y = y1 + bin_r * bin_h`. Within this bin, sample `sampling_ratio x sampling_ratio` evenly spaced points at `x = start_x + (sx + 0.5) * (bin_w / sampling_ratio)` and `y = start_y + (sy + 0.5) * (bin_h / sampling_ratio)` for `sy, sx in [0, ..., sampling_ratio-1]` in row-major order. Return list of `[round(x, 4), round(y, 4)]` pairs.

<PracticePlayground problemId="deep-learning-prob-39" />
