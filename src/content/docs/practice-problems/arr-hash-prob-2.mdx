---
title: "Practice: Contains Duplicate"
description: "Master #532. Arrays / Hashing / Two Pointers Module 1: Problem 2 in the Arrays / Hashing / Two Pointers track of the 1,000+ problem Neural Mastery AI Engineering curriculum."
difficulty: "easy"
topic: "Arrays / Hashing / Two Pointers"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# #532. Arrays / Hashing / Two Pointers Module 1: Problem 2

**Track**: Arrays / Hashing / Two Pointers | **Rank**: #532 of 1072 | **Points**: 40 XP

## Overview

Implement `contains_duplicate(nums)`, the simplest real use of a hash set: turning an O(n^2) all-pairs comparison into a single O(n) pass by tracking what has already been seen.

## Task

Implement the function `contains_duplicate(nums)`, returning `True` if any value appears at least twice in `nums`, `False` if every element is distinct.

<PracticePlayground problemId="arr-hash-prob-2" />
