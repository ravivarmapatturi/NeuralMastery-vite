---
title: "Practice: Vectorized Manhattan (L1) Pairwise Distance"
sidebar_position: 220
description: "Curriculum Problem 220 (Stage 4: NumPy & Vectorized Data Processing): Master NumPy / vectorization."
difficulty: medium
topic: numpy-pandas
---

import PracticePlayground from '../../../components/content/PracticePlayground';
import { Solution } from '../../../components/content/ExpandableDepth';

# NumPy Module 40: Problem 220

**Rank #220** · Difficulty: **Medium** · Stage: **Stage 4: NumPy & Vectorized Data Processing** · Category: **NumPy & Pandas**

## Overview

Compute the pairwise L1 Manhattan distance matrix between two feature sets.

## Task

Implement `pairwise_manhattan_distance(x, y)`: for matrix x of shape (N, D) and matrix y of shape (M, D), return the (N, M) matrix of pairwise Manhattan distances `sum(abs(x[i][k] - y[j][k]) for k in range(D))`. Round each float to 4 decimal places.

<PracticePlayground problemId="numpy-pandas-prob-40" />

<Solution>

```python
def pairwise_manhattan_distance(x, y):
    res = []
    for u in x:
        row = []
        for v in y:
            d = sum(abs(a - b) for a, b in zip(u, v))
            row.append(round(float(d), 4))
        res.append(row)
    return res
```

Manhattan distance measures city-block distance along coordinate axes, less sensitive to extreme coordinate outliers than Euclidean distance.

</Solution>
