---
title: "Practice: #200. Metadata Filtering Router"
description: "Master #200. Metadata Filtering Router in the RAG Fundamentals track of the 1,000+ problem Neural Mastery AI Engineering curriculum."
difficulty: "medium"
topic: "RAG Fundamentals"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# #200. Metadata Filtering Router

**Track**: RAG Fundamentals | **Rank**: #200 of 1072 | **Points**: 60 XP

## Overview

Extract structured SQL-like metadata constraints from search queries to filter vector databases.

## Task

Implement `metadata_filtering_router(query, available_filters)`. Scan `query` for filter expressions matching `field[op]value` where operator is one of `:`, `>`, `<`, `=`. If `field` is present in `available_filters`, parse `value` into its declared type (`int`, `float`, `bool`, or `string`), and record `{"field": field, "operator": op, "value": parsed_value}`. Strip all recognized filter tokens from the query, normalize excess whitespace in `clean_query`, and return `{"filters": filters, "clean_query": clean_query}` sorted alphabetically by field.

<PracticePlayground problemId="rag-fund-prob-20" />
