---
title: "Practice: Cosine Annealing Learning Rate"
description: "Implement cosine-annealing learning rate decay, one of the most widely-used real LR schedules for training large models."
difficulty: "medium"
topic: "MLOps & Data Systems"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Cosine Annealing Learning Rate

**Track**: MLOps & Data Systems | **Points**: 40 XP

## Overview

Implement cosine-annealing learning rate decay, one of the most widely-used real LR schedules for training large models.

## Task

Implement `cosine_annealing_lr(step, total_steps, base_lr, min_lr=0.0)`. Return `min_lr + 0.5*(base_lr-min_lr)*(1 + cos(pi*step/total_steps))`. Clamp `step` to `total_steps` if it exceeds it (so the schedule never goes below `min_lr`).

<PracticePlayground problemId="mlops-sys-prob-14" />
