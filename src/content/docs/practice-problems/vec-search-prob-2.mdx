---
title: "Practice: Manhattan Distance Between Vectors"
description: "Master Manhattan Distance Between Vectors in the Vector Search & Index Optimization track of the 1,000+ problem Neural Mastery AI Engineering curriculum."
difficulty: "easy"
topic: "Vector Search & Index Optimization"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# #977. Vector Search & Index Optimization Module 1: Problem 2

**Track**: Vector Search & Index Optimization | **Rank**: #977 of 1072 | **Points**: 40 XP

## Overview

Implement `manhattan_distance(a, b)`, the L1 distance metric -- the axis-aligned alternative to Euclidean distance that some vector indexes expose when sparse or grid-like structure in the embedding matters more than straight-line distance.

## Task

Implement the function `manhattan_distance(a, b)`, returning `sum(abs(a_i - b_i))` over two equal-length numeric vectors `a` and `b`. Raise `ValueError` if `len(a) != len(b)`.

<PracticePlayground problemId="vec-search-prob-2" />
