---
title: "Practice: Cross-Entropy Loss for Next-Token Prediction"
description: "Implement cross-entropy loss, the real, standard training objective for next-token prediction in language models."
difficulty: "medium"
topic: "Transformers & LLMs"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Cross-Entropy Loss for Next-Token Prediction

**Track**: Transformers & LLMs | **Points**: 40 XP

## Overview

Implement cross-entropy loss, the real, standard training objective for next-token prediction in language models.

## Task

Implement `cross_entropy_loss(predicted_probs, target_idx)`: return `-log(predicted_probs[target_idx])`.

<PracticePlayground problemId="llm-internals-prob-56" />
