---
title: "Practice: Vision Transformer CLS Token Prepending"
description: "Prepend the learnable [CLS] classification token to the sequence of patch embeddings in Vision Transformers."
difficulty: "easy"
topic: "Deep Learning & Vision/NLP"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Vision Transformer CLS Token Prepending

**Track**: Deep Learning & Vision/NLP | **Points**: 40 XP

## Overview

Prepend the learnable [CLS] classification token to the sequence of patch embeddings in Vision Transformers.

## Task

Implement `vit_class_token_prepend(patch_embeddings, cls_token)`. Given a 2D matrix `patch_embeddings` of shape `N x D` and a 1D vector `cls_token` of length `D`, insert `cls_token` as the very first row (position 0), resulting in an `(N + 1) x D` matrix. Return the new 2D list.

<PracticePlayground problemId="deep-learning-prob-60" />
