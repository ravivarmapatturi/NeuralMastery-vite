---
title: "Practice: Valid Anagram"
description: "Master Valid Anagram in the Arrays / Hashing / Two Pointers track of the 1,000+ problem Neural Mastery AI Engineering curriculum."
difficulty: "easy"
topic: "Arrays / Hashing / Two Pointers"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# #533. Arrays / Hashing / Two Pointers Module 1: Problem 3

**Track**: Arrays / Hashing / Two Pointers | **Rank**: #533 of 1072 | **Points**: 40 XP

## Overview

Implement `is_anagram(s, t)`, anagram detection via character-frequency counting -- the general hash-map pattern for "do two collections hold the exact same multiset of items?"

## Task

Implement the function `is_anagram(s, t)`, returning `True` if `t` is an anagram of `s` (same characters, same multiplicity, any order), `False` otherwise.

<PracticePlayground problemId="arr-hash-prob-3" />
