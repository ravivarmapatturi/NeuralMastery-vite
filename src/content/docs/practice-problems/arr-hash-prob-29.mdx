---
title: "Practice: Smallest Range Covering Elements from K Lists"
description: "Master Smallest Range Covering Elements from K Lists in the Arrays / Hashing / Two Pointers track of the 1,000+ problem Neural Mastery AI Engineering curriculum."
difficulty: "hard"
topic: "Arrays / Hashing / Two Pointers"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# #559. Smallest Range Covering Elements from K Lists

**Track**: Arrays / Hashing / Two Pointers | **Rank**: #559 of 1072 | **Points**: 90 XP

## Overview

Implement `smallest_range(nums)` by orchestrating a min-heap across k sorted lists to find the tightest interval spanning all lists simultaneously.

## Task

Implement the function `smallest_range(nums)`: return the smallest range `[start, end]` (inclusive) containing at least one element from every list in `nums`.

<PracticePlayground problemId="arr-hash-prob-29" />
