---
title: "Practice: Retrieve-Read-Retrieve Loop"
description: "Master #182. Retrieve-Read-Retrieve Loop in the RAG Fundamentals track of the 1,000+ problem Neural Mastery AI Engineering curriculum."
difficulty: "easy"
topic: "RAG Fundamentals"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# #182. Retrieve-Read-Retrieve Loop

**Track**: RAG Fundamentals | **Rank**: #182 of 1072 | **Points**: 40 XP

## Overview

Orchestrate iterative multi-turn RAG reasoning to resolve queries requiring intermediate evidence.

## Task

Implement `retrieve_read_retrieve_loop(question, step_data, max_steps=3)`. Begin with `current_query = question`. For each step up to `max_steps`, look up `current_query` in `step_data` (case-insensitive query match). If `found_answer` is present, return `{"status": "success", "steps": current_step, "answer": found_answer, "history": history}`. If `next_query` is present and not already in `history`, update `current_query` and iterate. If unresolvable or looping, terminate and return status `"incomplete"`.

<PracticePlayground problemId="rag-fund-prob-2" />
