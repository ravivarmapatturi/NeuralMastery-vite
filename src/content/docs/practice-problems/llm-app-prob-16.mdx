---
title: "Practice: Prompt Injection Rule & Keyword Scanner"
description: "Master Prompt Injection Rule & Keyword Scanner in the LLM Application Engineering track of the 1,000+ problem Neural Mastery AI Engineering curriculum."
difficulty: "medium"
topic: "LLM Application Engineering"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# #076. Prompt Injection Rule & Keyword Scanner

**Track**: LLM Application Engineering | **Rank**: #76 of 1072 | **Points**: 60 XP

## Overview

Guard against jailbreak attacks and system prompt leakage by scanning user prompts for injection signatures and control delimiters. In production LLM application engineering, reliable system components, deterministic interfaces, and defensive validation are paramount for production stability and cost efficiency.

## Task

Implement `detect_prompt_injection(prompt, custom_blocklist=None)`. Check `prompt` (case-insensitively) against built-in injection signatures (`'ignore previous instructions'`, `'disregard all previous'`, `'system prompt reveal'`, `'dan mode'`, `'developer mode'`, `'<|im_start|>'`, `'<|system|>'`, `'[inst]'`) plus any phrases in `custom_blocklist`. Collect all matched patterns sorted alphabetically. Compute `risk_score = min(1.0, round(0.35 * len(matched_patterns), 2))`. Set `is_flagged = True` if any pattern matches. Return `{'is_flagged': bool, 'risk_score': float, 'matched_patterns': list[str]}`.

<PracticePlayground problemId="llm-app-prob-16" />
