---
title: "Practice: Median of Two Sorted Arrays"
description: "Master Median of Two Sorted Arrays in the Arrays / Hashing / Two Pointers track of the 1,000+ problem Neural Mastery AI Engineering curriculum."
difficulty: "hard"
topic: "Arrays / Hashing / Two Pointers"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# #554. Arrays / Hashing / Two Pointers Module 5: Problem 24

**Track**: Arrays / Hashing / Two Pointers | **Rank**: #554 of 1072 | **Points**: 90 XP

## Overview

Implement `find_median_sorted_arrays(nums1, nums2)`, the binary-search-on-partition-point solution -- one of the most-cited "hard" interview problems, solved by binary searching for a partition rather than ever merging the two arrays.

## Task

Implement the function `find_median_sorted_arrays(nums1, nums2)`: both arrays are already sorted ascending. Return the median of the combined (conceptually merged) sorted array, as a float, in O(log(min(len(nums1), len(nums2)))) time.

<PracticePlayground problemId="arr-hash-prob-24" />
