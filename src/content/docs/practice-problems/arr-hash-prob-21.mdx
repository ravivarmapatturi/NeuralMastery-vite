---
title: "Practice: Trapping Rain Water"
description: "Master Trapping Rain Water in the Arrays / Hashing / Two Pointers track of the 1,000+ problem Neural Mastery AI Engineering curriculum."
difficulty: "medium"
topic: "Arrays / Hashing / Two Pointers"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# #551. Arrays / Hashing / Two Pointers Module 5: Problem 21

**Track**: Arrays / Hashing / Two Pointers | **Rank**: #551 of 1072 | **Points**: 60 XP

## Overview

Implement `trap_rain_water(height)` via precomputed left-max/right-max arrays -- the classic hard array problem, keyed on the insight that water trapped at any position depends only on the shorter of the tallest walls on either side.

## Task

Implement the function `trap_rain_water(height)`: `height[i]` is the height of a bar at position `i` in an elevation map. Return the total units of rainwater trapped between the bars after it rains.

<PracticePlayground problemId="arr-hash-prob-21" />
