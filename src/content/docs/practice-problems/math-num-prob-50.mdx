---
title: "Practice: PageRank Power Iteration Step"
sidebar_position: 170
description: "Curriculum Problem 170 (Stage 3: Mathematics & Numerical Computing): Master Mathematics / numerical computing."
difficulty: medium
topic: math-numerical
---

import PracticePlayground from '../../../components/content/PracticePlayground';
import { Solution } from '../../../components/content/ExpandableDepth';

# Mathematics Module 50: Problem 170

**Rank #170** · Difficulty: **Medium** · Stage: **Stage 3: Mathematics & Numerical Computing** · Category: **Math & Numerical**

## Overview

Execute a single PageRank transition step over a directed link graph.

## Task

Implement `pagerank_step(adj_matrix, p, damping=0.85)`: normalize columns of `adj_matrix` (where `adj[i][j]` is link from j to i) so column sums are 1. If a column sum is 0 (dangling node), replace column entries with `1/n`. Compute `p_next = damping * M * p + (1 - damping) / n`. Return probability vector rounded to 4 decimals.

<PracticePlayground problemId="math-num-prob-50" />

<Solution>

```python
def pagerank_step(adj_matrix, p, damping=0.85):
    n = len(p)
    col_sums = [sum(adj_matrix[i][j] for i in range(n)) for j in range(n)]
    M = [[0.0] * n for _ in range(n)]
    for j in range(n):
        if col_sums[j] > 0:
            for i in range(n):
                M[i][j] = adj_matrix[i][j] / col_sums[j]
        else:
            for i in range(n):
                M[i][j] = 1.0 / n
    Mp = [sum(M[i][j] * p[j] for j in range(n)) for i in range(n)]
    base = (1.0 - damping) / n
    p_next = [damping * Mp[i] + base for i in range(n)]
    return [round(float(v), 4) for v in p_next]
```

Constructs stochastic transition matrix with dangling node teleports and blends with uniform random jump.

</Solution>
