---
title: "Practice: Spatial Transformer Network Normalized Grid Generator"
description: "Generate normalized source sampling coordinates [-1.0, 1.0] for a Spatial Transformer Network (STN)."
difficulty: "medium"
topic: "Deep Learning & Vision/NLP"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Spatial Transformer Network Normalized Grid Generator

**Track**: Deep Learning & Vision/NLP | **Points**: 40 XP

## Overview

Generate normalized source sampling coordinates [-1.0, 1.0] for a Spatial Transformer Network (STN).

## Task

Implement `spatial_transform_grid(theta, out_h, out_w)`. For output grid coordinates `(r, c)`, normalized target coordinates are `yt = -1.0 + 2.0 * r / (out_h - 1)` (if `out_h > 1` else 0.0) and `xt = -1.0 + 2.0 * c / (out_w - 1)` (if `out_w > 1` else 0.0). Apply affine transform: `xs = theta[0][0] * xt + theta[0][1] * yt + theta[0][2]` and `ys = theta[1][0] * xt + theta[1][1] * yt + theta[1][2]`. Return 3D list of shape `out_h x out_w x 2` where cell is `[round(xs, 4), round(ys, 4)]`.

<PracticePlayground problemId="deep-learning-prob-55" />
