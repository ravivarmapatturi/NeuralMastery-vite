---
title: "Practice: Assign Nodes to Visualization Layers"
description: "Implement layer assignment for agent-graph visualization, computing each node's vertical position (its longest-path depth from any entry node) for a clean layered layout."
difficulty: "hard"
topic: "Agent Graph Engineering"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Assign Nodes to Visualization Layers

**Track**: Agent Graph Engineering | **Points**: 40 XP

## Overview

Implement layer assignment for agent-graph visualization, computing each node's vertical position (its longest-path depth from any entry node) for a clean layered layout.

## Task

Implement `assign_layers(dependencies)`. Each node's layer is `0` if it has no dependencies, otherwise `1 + max(layer of each dependency)`. Return a dict of node -> layer (int).

<PracticePlayground problemId="agent-graph-prob-22" />
