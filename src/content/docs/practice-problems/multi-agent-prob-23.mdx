---
title: "Practice: Distributed Lock Lease Expiry Check"
description: "Implement distributed-lock lease-expiry checking, the real safety mechanism preventing a crashed agent from holding a lock forever."
difficulty: "easy"
topic: "Multi-Agent Systems"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Distributed Lock Lease Expiry Check

**Track**: Multi-Agent Systems | **Points**: 40 XP

## Overview

Implement distributed-lock lease-expiry checking, the real safety mechanism preventing a crashed agent from holding a lock forever.

## Task

Implement `is_lease_expired(acquired_at, current_time, lease_duration_s)`: return `True` if `(current_time - acquired_at) >= lease_duration_s`.

<PracticePlayground problemId="multi-agent-prob-23" />
