---
title: "Practice: Build a Tool-Call Audit Log Entry"
description: "Implement audit-log entry construction, the real record-keeping every production agent system needs for tool calls it makes on a user's behalf."
difficulty: "easy"
topic: "MCP Advanced & Agent Integration"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Build a Tool-Call Audit Log Entry

**Track**: MCP Advanced & Agent Integration | **Points**: 40 XP

## Overview

Implement audit-log entry construction, the real record-keeping every production agent system needs for tool calls it makes on a user's behalf.

## Task

Implement `build_audit_entry(tool_name, arguments, result_status, timestamp)`. Return `{"tool": tool_name, "args": arguments, "status": result_status, "ts": timestamp}` -- exactly these 4 keys, no more, no less.

<PracticePlayground problemId="mcp-adv-prob-13" />
