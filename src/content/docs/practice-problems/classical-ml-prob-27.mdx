---
title: "Practice: Classical ML Module 27: Problem 267"
sidebar_position: 267
description: "Curriculum Problem 267 (Stage 5: Classical Machine Learning & Optimization): Master Classical ML."
difficulty: medium
topic: classical-ml
---

import PracticePlayground from '../../../components/content/PracticePlayground';
import { Solution } from '../../../components/content/ExpandableDepth';

# Classical ML Module 27: Problem 267

**Rank #267** · Difficulty: **Medium** · Stage: **Stage 5: Classical Machine Learning & Optimization** · Category: **Classical ML**

## Overview

Find the optimal binary split point on a 1D continuous feature minimizing weighted Gini impurity.

## Task

Implement `best_split_threshold(feature_values, labels)`. Sort unique feature values; evaluate candidate thresholds midway between adjacent sorted values. Find threshold minimizing `(|left|/N)*Gini(left) + (|right|/N)*Gini(right)`. Return `(best_threshold, min_weighted_gini)`, rounded to 4 decimal places.

<PracticePlayground problemId="classical-ml-prob-27" />

<Solution>

```python
from collections import Counter

def best_split_threshold(feature_values, labels):
    def gini(lbls):
        if not lbls:
            return 0.0
        cnts = Counter(lbls)
        n = len(lbls)
        return 1.0 - sum((c / n) ** 2 for c in cnts.values())

    pairs = sorted(zip(feature_values, labels), key=lambda x: x[0])
    vals = [p[0] for p in pairs]
    unique_vals = sorted(list(set(vals)))
    if len(unique_vals) <= 1:
        return unique_vals[0] if unique_vals else 0.0, 0.0

    n = len(pairs)
    best_gini = float('inf')
    best_thresh = unique_vals[0]
    for i in range(len(unique_vals) - 1):
        mid = (unique_vals[i] + unique_vals[i + 1]) / 2.0
        left = [lbl for v, lbl in pairs if v <= mid]
        right = [lbl for v, lbl in pairs if v > mid]
        weighted = (len(left) / n) * gini(left) + (len(right) / n) * gini(right)
        if weighted < best_gini:
            best_gini = weighted
            best_thresh = mid
    return round(float(best_thresh), 4), round(float(best_gini), 4)
```

Evaluating midpoints between sorted unique continuous values ensures exhaustive search over all possible discrete bipartitions.

</Solution>
