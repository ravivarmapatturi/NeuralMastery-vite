---
title: "Practice: GloVe Co-occurrence Weighting Function"
description: "Evaluate the GloVe sub-linear weighting term preventing very frequent co-occurrences from dominating embedding training."
difficulty: "easy"
topic: "Deep Learning & Vision/NLP"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# GloVe Co-occurrence Weighting Function

**Track**: Deep Learning & Vision/NLP | **Points**: 40 XP

## Overview

Evaluate the GloVe sub-linear weighting term preventing very frequent co-occurrences from dominating embedding training.

## Task

Implement `glove_weighting_function(x, x_max, alpha)`. If `x < x_max`, return `(x / x_max) ** alpha`. Otherwise, return `1.0`. Round result to 4 decimal places.

<PracticePlayground problemId="deep-learning-prob-18" />
