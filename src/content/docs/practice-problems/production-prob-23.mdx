---
title: "Practice: Health Check Failure Debouncing"
description: "Implement health-check failure debouncing, a real requirement to avoid a load balancer flapping an instance in and out of rotation on a single transient failed check."
difficulty: "medium"
topic: "Agent Performance & Production"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Health Check Failure Debouncing

**Track**: Agent Performance & Production | **Points**: 40 XP

## Overview

Implement health-check failure debouncing, a real requirement to avoid a load balancer flapping an instance in and out of rotation on a single transient failed check.

## Task

Implement `is_unhealthy(consecutive_failed_checks, unhealthy_threshold)`: return `True` if `consecutive_failed_checks >= unhealthy_threshold`.

<PracticePlayground problemId="production-prob-23" />
