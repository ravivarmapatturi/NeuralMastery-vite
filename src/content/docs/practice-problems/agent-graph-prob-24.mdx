---
title: "Practice: Determine Remaining Nodes to Execute From a Checkpoint"
description: "Implement checkpoint-resume node selection, determining exactly which nodes still need to run after resuming an agent graph from a saved checkpoint (avoiding re-running completed work)."
difficulty: "medium"
topic: "Agent Graph Engineering"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Determine Remaining Nodes to Execute From a Checkpoint

**Track**: Agent Graph Engineering | **Points**: 40 XP

## Overview

Implement checkpoint-resume node selection, determining exactly which nodes still need to run after resuming an agent graph from a saved checkpoint (avoiding re-running completed work).

## Task

Implement `remaining_from_checkpoint(execution_order, resume_from)`. `execution_order` is the full planned sequence. Return the sublist STARTING FROM (and including) `resume_from`. Return an empty list if `resume_from` isn't in `execution_order`.

<PracticePlayground problemId="agent-graph-prob-24" />
