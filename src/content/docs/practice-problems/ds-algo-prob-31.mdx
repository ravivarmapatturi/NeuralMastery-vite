---
title: "Practice: Reorder List"
description: "Implement Linked Lists: Reorder List in the Data Structures & Algorithms track of the 1,000+ problem Neural Mastery AI Engineering curriculum."
difficulty: "medium"
topic: "data-structures-algorithms"
sidebar_position: 91
---

import PracticePlayground from '../../../components/content/PracticePlayground';
import { Solution } from '../../../components/content/ExpandableDepth';

# Linked Lists: Reorder List

**Rank #91** · Difficulty: **Medium** · Stage: **Stage 2: Data Structures & Algorithmic Foundations** · Category: **Data Structures & Algorithms**

## Overview

Implement `reorder_list(head)` -- find the middle, reverse the second half, then interleave. Combines three classic linked-list techniques in one problem.

## Task

Implement the function `reorder_list(head)`. Reorder in-place: `L0 -> L1 -> ... -> Ln-1 -> Ln` becomes `L0 -> Ln -> L1 -> Ln-1 -> L2 -> Ln-2 -> ...`. Return the new head.

<PracticePlayground problemId="ds-algo-prob-31" />

<Solution>

```python
def reorder_list(head):
    if not head or not head['next']:
        return head
    # 1. Find middle (slow/fast pointers)
    slow, fast = head, head
    while fast['next'] and fast['next']['next']:
        slow = slow['next']
        fast = fast['next']['next']
    # 2. Reverse second half
    second = slow['next']
    slow['next'] = None
    prev = None
    cur = second
    while cur:
        nxt = cur['next']
        cur['next'] = prev
        prev = cur
        cur = nxt
    second = prev
    # 3. Interleave
    first = head
    while second:
        tmp1, tmp2 = first['next'], second['next']
        first['next'] = second
        second['next'] = tmp1
        first, second = tmp1, tmp2
    return head
```

Three phases: find mid via slow/fast, reverse second half, interleave. All O(n) time, O(1) space.

</Solution>
