---
title: "Practice: Output-Length Denial-of-Service Guard"
description: "Implement an output-length guard, a real, simple defense against a request asking for an unreasonably large generation that would tie up compute or spike cost."
difficulty: "easy"
topic: "Agent Security & Reliability"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Output-Length Denial-of-Service Guard

**Track**: Agent Security & Reliability | **Points**: 40 XP

## Overview

Implement an output-length guard, a real, simple defense against a request asking for an unreasonably large generation that would tie up compute or spike cost.

## Task

Implement `is_output_length_safe(requested_max_tokens, hard_ceiling)`: return `True` if `0 < requested_max_tokens <= hard_ceiling`.

<PracticePlayground problemId="security-prob-15" />
