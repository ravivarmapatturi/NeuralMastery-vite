---
title: "Practice: #93. Graphs: Pacific Atlantic Water Flow"
description: "Implement Graphs: Pacific Atlantic Water Flow in the Data Structures & Algorithms track of the 1,000+ problem Neural Mastery AI Engineering curriculum."
difficulty: "medium"
topic: "data-structures-algorithms"
sidebar_position: 93
---

import PracticePlayground from '../../../components/content/PracticePlayground';
import { Solution } from '../../../components/content/ExpandableDepth';

# Graphs: Pacific Atlantic Water Flow

**Rank #93** · Difficulty: **Medium** · Stage: **Stage 2: Data Structures & Algorithmic Foundations** · Category: **Data Structures & Algorithms**

## Overview

Implement `pacific_atlantic(heights)` -- reverse BFS from both oceans. Thinking backwards (water flows *up* from oceans to identify reachable cells) is the key insight.

## Task

Implement the function `pacific_atlantic(heights)`. Return all cells `[r, c]` from which water can flow to both the Pacific and Atlantic oceans.

<PracticePlayground problemId="ds-algo-prob-33" />

<Solution>

```python
from collections import deque

def pacific_atlantic(heights):
    if not heights:
        return []
    rows, cols = len(heights), len(heights[0])

    def bfs(starts):
        visited = set(starts)
        q = deque(starts)
        while q:
            r, c = q.popleft()
            for dr, dc in [(0,1),(0,-1),(1,0),(-1,0)]:
                nr, nc = r+dr, c+dc
                if 0<=nr<rows and 0<=nc<cols and (nr,nc) not in visited and heights[nr][nc]>=heights[r][c]:
                    visited.add((nr,nc))
                    q.append((nr,nc))
        return visited

    pacific = bfs([(0,c) for c in range(cols)] + [(r,0) for r in range(rows)])
    atlantic = bfs([(rows-1,c) for c in range(cols)] + [(r,cols-1) for r in range(rows)])
    return sorted([list(pos) for pos in pacific & atlantic])
```

BFS from ocean borders inward (water flows *up*): cells reachable from both oceans are the answer. Set intersection finds the overlap. O(m*n) time.

</Solution>
