---
title: "Practice: #783. Pandas & Data Processing Pipelines Module 3: Problem 13"
description: "Master #783. Pandas & Data Processing Pipelines Module 3: Problem 13 in the Pandas & Data Processing Pipelines track of the 1,000+ problem Neural Mastery AI Engineering curriculum."
difficulty: "medium"
topic: "Pandas & Data Processing Pipelines"
---

import PracticePlayground from '../../../components/content/PracticePlayground';
import { Solution } from '../../../components/content/ExpandableDepth';

# #783. Time-Window Event Stream Sessionization

**Track**: Pandas & Data Processing Pipelines | **Rank**: #783 of 1072 | **Points**: 60 XP

## Overview

Segment raw user interaction clickstreams into bounded continuous activity sessions.

## Task

Implement `sessionize_user_events(events, inactivity_timeout)`: sort events by `(user_id, timestamp)`. For each user, if the difference between successive timestamps exceeds `inactivity_timeout`, increment the user's session counter. Assign `session_id = f'{user_id}_s{counter}'` to each event record. Return annotated event records.

<PracticePlayground problemId="pandas-proc-prob-13" />

<Solution>

```python
from collections import defaultdict

def sessionize_user_events(events, inactivity_timeout):
    out = []
    sess_count = defaultdict(int)
    last_ts = {}
    for ev in sorted(events, key=lambda x: (x['user_id'], x['timestamp'])):
        uid, ts = ev['user_id'], ev['timestamp']
        if uid not in last_ts or (ts - last_ts[uid]) > inactivity_timeout:
            sess_count[uid] += 1
        row = dict(ev)
        row['session_id'] = f'{uid}_s{sess_count[uid]}'
        out.append(row)
        last_ts[uid] = ts
    return out
```

Tracks per-user inactivity gaps and assigns new session identifiers when elapsed time exceeds timeout.

</Solution>
