---
title: "Practice: Top-K Logit Filtering"
description: "Implement Top-K logit filtering -- pruning low-probability tail tokens from the candidate vocabulary during autoregressive generation."
difficulty: "easy"
topic: "transformers-llms"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Top-K Logit Filtering

**Track**: transformers-llms | **Points**: 40 XP

## Overview

Implement Top-K logit filtering -- pruning low-probability tail tokens from the candidate vocabulary during autoregressive generation.

## Task

Implement `top_k_filter(logits, k, mask_value=-1e9)`. Identify the top `k` highest logit positions in `logits`. Keep their original values while setting all remaining positions to `mask_value`.

<PracticePlayground problemId="llm-internals-prob-13" />
