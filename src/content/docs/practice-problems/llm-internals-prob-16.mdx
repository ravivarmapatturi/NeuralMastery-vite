---
title: "Practice: Transformer Attention Head Dimension"
description: "Compute the per-head key/query/value dimension in multi-head attention given d_model and num_heads."
difficulty: "easy"
topic: "transformers-llms"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Transformer Attention Head Dimension

**Track**: transformers-llms | **Points**: 40 XP

## Overview

Compute the per-head key/query/value dimension in multi-head attention given d_model and num_heads.

## Task

Implement `attention_head_dim(d_model, num_heads)`. Compute the per-head dimension `d_k = d_model // num_heads` used in multi-head attention. Raise `ValueError` if either parameter is non-positive or if `d_model` is not divisible by `num_heads`.

<PracticePlayground problemId="llm-internals-prob-16" />
