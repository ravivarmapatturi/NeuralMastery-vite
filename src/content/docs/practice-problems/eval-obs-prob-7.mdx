---
title: "Practice: Detect Test-Set Contamination via Overlap"
description: "Implement a real, simple contamination check, flagging when eval benchmark examples appear to overlap with training data -- a genuine, common cause of inflated eval scores that don't reflect real generalization."
difficulty: "medium"
topic: "Agent Evaluation & Observability"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Detect Test-Set Contamination via Overlap

**Track**: Agent Evaluation & Observability | **Points**: 40 XP

## Overview

Implement a real, simple contamination check, flagging when eval benchmark examples appear to overlap with training data -- a genuine, common cause of inflated eval scores that don't reflect real generalization.

## Task

Implement `contamination_overlap(eval_examples, training_examples)`: return the fraction of `eval_examples` that appear EXACTLY (as an element) in `training_examples`.

<PracticePlayground problemId="eval-obs-prob-7" />
