---
title: "Practice: MobileNetV2 Inverted Residual Block Architecture Specs"
description: "Master #920. Computer Vision & CNN Architectures Module 6: Problem 30 in the Computer Vision & CNN Architectures track of the 1,000+ problem Neural Mastery AI Engineering curriculum."
difficulty: "medium"
topic: "Computer Vision & CNN Architectures"
---

import PracticePlayground from '../../../components/content/PracticePlayground';
import { Solution } from '../../../components/content/ExpandableDepth';

# #920. MobileNetV2 Inverted Residual Block Architecture Specs

**Track**: Computer Vision & CNN Architectures | **Rank**: #920 of 1072 | **Points**: 60 XP

## Overview

Design lightweight mobile vision backbones using inverted residual structures with linear bottlenecks and depthwise separable convolutions.

## Task

Implement `inverted_residual_block_specs(...)`. The MBConv block structure consists of:\n1. Expansion 1x1 Conv: projects `in_channels` to `expanded_channels = in_channels * expand_ratio`. If `expand_ratio == 1`, this layer is omitted (0 weights), otherwise it has `in_channels * expanded_channels` weights.\n2. Depthwise 3x3 Conv: operates per channel, having `expanded_channels * 9` weights.\n3. Linear Bottleneck 1x1 Conv: projects back to `out_channels`, having `expanded_channels * out_channels` weights.\n4. Residual connection: present if and only if `stride == 1 and in_channels == out_channels`.\nReturn dict `{"expanded_channels": expanded_channels, "has_residual": has_residual, "total_weights": total_weights}`.

<PracticePlayground problemId="cv-cnn-prob-30" />

<Solution>

```python
def inverted_residual_block_specs(in_channels: int, out_channels: int, stride: int, expand_ratio: int) -> dict:
    expanded_channels = in_channels * expand_ratio
    has_residual = (stride == 1) and (in_channels == out_channels)
    expand_weights = in_channels * expanded_channels if expand_ratio != 1 else 0
    depthwise_weights = expanded_channels * 9
    project_weights = expanded_channels * out_channels
    total_weights = expand_weights + depthwise_weights + project_weights
    return {
        "expanded_channels": expanded_channels,
        "has_residual": has_residual,
        "total_weights": total_weights
    }
```

Accounting for conditional expansion, channel-wise depthwise kernels, and projection weights quantifies the parameter footprint.

</Solution>
