---
title: "Practice: Reservoir Sampling Algorithm Step"
sidebar_position: 43
description: "Simulate a single transition step of Jeffrey Vitter's Reservoir Sampling Algorithm R."
difficulty: medium
topic: python-fundamentals
---

import PracticePlayground from '../../../components/content/PracticePlayground';
import { Solution } from '../../../components/content/ExpandableDepth';

# Reservoir Sampling Algorithm Step

**Rank #43** · Difficulty: **Medium** · Stage: **Stage 1: AI Foundations & Python Programming** · Category: **Python & Fundamentals**

## Overview

Simulate a single transition step of Jeffrey Vitter's Reservoir Sampling Algorithm R.

## Task

Implement `reservoir_sample_step(current_sample, item, item_index, random_val, k)`: given the current reservoir sample list of capacity `k`, process `item` arriving at 1-based `item_index` with deterministic random draw `random_val` in `[0.0, 1.0)`. If `item_index <= k`, append item. Otherwise, compute candidate replacement index `int(random_val * item_index)`; if that index is `< k`, replace the element at that index with `item`. Return a new updated sample list copy.

<PracticePlayground problemId="py-foundations-prob-43" />

<Solution>

```python
def reservoir_sample_step(current_sample, item, item_index, random_val, k):
    res = list(current_sample)
    if item_index <= k:
        res.append(item)
    else:
        replace_idx = int(random_val * item_index)
        if replace_idx < k:
            res[replace_idx] = item
    return res
```

Algorithm R guarantees every stream element has an exact k/N probability of appearing in the final reservoir.

</Solution>
