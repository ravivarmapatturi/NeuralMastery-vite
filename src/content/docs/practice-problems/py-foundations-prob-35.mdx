---
title: "Practice: Matrix Spiral Traversal"
sidebar_position: 35
description: "Unroll a 2D matrix into a 1D sequence in clockwise concentric spiral boundary order."
difficulty: medium
topic: python-fundamentals
---

import PracticePlayground from '../../../components/content/PracticePlayground';
import { Solution } from '../../../components/content/ExpandableDepth';

# Matrix Spiral Traversal

**Rank #35** · Difficulty: **Medium** · Stage: **Stage 1: AI Foundations & Python Programming** · Category: **Python & Fundamentals**

## Overview

Unroll a 2D matrix into a 1D sequence in clockwise concentric spiral boundary order.

## Task

Implement `spiral_order(matrix)`: traverse an `m x n` matrix in clockwise spiral order starting from `(0, 0)` and proceeding right, down, left, up, then repeating inwards until all elements are visited.

<PracticePlayground problemId="py-foundations-prob-35" />

<Solution>

```python
def spiral_order(matrix):
    if not matrix or not matrix[0]:
        return []
    res = []
    top, bottom = 0, len(matrix) - 1
    left, right = 0, len(matrix[0]) - 1
    while top <= bottom and left <= right:
        for c in range(left, right + 1):
            res.append(matrix[top][c])
        top += 1
        for r in range(top, bottom + 1):
            res.append(matrix[r][right])
        right -= 1
        if top <= bottom:
            for c in range(right, left - 1, -1):
                res.append(matrix[bottom][c])
            bottom -= 1
        if left <= right:
            for r in range(bottom, top - 1, -1):
                res.append(matrix[r][left])
            left += 1
    return res
```

Incrementing top/left and decrementing bottom/right coordinates after each row/column traversal guarantees single-pass O(M*N) extraction.

</Solution>
