---
title: "Practice: Mathematics Module 45: Problem 165"
sidebar_position: 165
description: "Curriculum Problem 165 (Stage 3: Mathematics & Numerical Computing): Master Mathematics / numerical computing."
difficulty: medium
topic: math-numerical
---

import PracticePlayground from '../../../components/content/PracticePlayground';
import { Solution } from '../../../components/content/ExpandableDepth';

# Mathematics Module 45: Problem 165

**Rank #165** · Difficulty: **Medium** · Stage: **Stage 3: Mathematics & Numerical Computing** · Category: **Math & Numerical**

## Overview

Estimate expected values under an intractable target distribution using proposal samples and likelihood ratios.

## Task

Implement `importance_sampling(f_evals, p_densities, q_densities)`: compute likelihood ratio weights `w_i = p_i / q_i`. Calculate self-normalized estimate `mu_hat = sum(w_i * f_i) / sum(w_i)` and Kish's effective sample size `ESS = (sum(w_i))^2 / sum(w_i^2)`. Return dict rounded to 4 decimals.

<PracticePlayground problemId="math-num-prob-45" />

<Solution>

```python
def importance_sampling(f_evals, p_densities, q_densities):
    weights = [p / q for p, q in zip(p_densities, q_densities)]
    sum_w = sum(weights)
    if sum_w < 1e-12:
        return {'estimate': 0.0, 'effective_sample_size': 0.0}
    estimate = sum(w * f for w, f in zip(weights, f_evals)) / sum_w
    sum_w_sq = sum(w ** 2 for w in weights)
    ess = (sum_w ** 2) / sum_w_sq if sum_w_sq > 1e-12 else 0.0
    return {'estimate': round(float(estimate), 4), 'effective_sample_size': round(float(ess), 4)}
```

Reweights proposal evaluations by Radon-Nikodym derivative and computes Kish's ESS.

</Solution>
