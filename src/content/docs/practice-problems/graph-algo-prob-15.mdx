---
title: "Practice: #635. Graphs & Network Algorithms Module 3: Problem 15"
description: "Master #635. Graphs & Network Algorithms Module 3: Problem 15 in the Graphs & Network Algorithms track of the 1,000+ problem Neural Mastery AI Engineering curriculum."
difficulty: "medium"
topic: "Graphs & Network Algorithms"
---

import PracticePlayground from '../../../components/content/PracticePlayground';
import { Solution } from '../../../components/content/ExpandableDepth';

# #635. Graphs & Network Algorithms Module 3: Problem 15

**Track**: Graphs & Network Algorithms | **Rank**: #635 of 1072 | **Points**

## Overview

Find shortest paths from a source to all nodes in a weighted graph using Dijkstra's algorithm.

## Task

Implement `dijkstra_shortest_paths(n, edges, src)`: use a priority queue (min-heap) to explore nodes in order of current shortest tentative distance.

<PracticePlayground problemId="graph-algo-prob-15" />

<Solution>

```python
import heapq
from collections import defaultdict

def dijkstra_shortest_paths(n, edges, src):
    adj = defaultdict(list)
    for u, v, w in edges:
        adj[u].append((v, w))
        adj[v].append((u, w))
    dist = {i: float('inf') for i in range(n)}
    dist[src] = 0
    pq = [(0, src)]
    while pq:
        d, u = heapq.heappop(pq)
        if d > dist[u]:
            continue
        for v, w in adj[u]:
            if dist[u] + w < dist[v]:
                dist[v] = dist[u] + w
                heapq.heappush(pq, (dist[v], v))
    return {k: (v if v != float('inf') else -1) for k, v in dist.items()}
```

Use a min-heap to greedily expand the vertex with the lowest tentative path distance. Relax neighboring edges whenever a shorter path is discovered.

</Solution>
