---
title: "Practice: Cluster Purity Score"
description: "Implement cluster purity, an external validation metric that scores clustering quality against known ground-truth labels."
difficulty: "medium"
topic: "Clustering & Recommender Systems"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Cluster Purity Score

**Track**: Clustering & Recommender Systems | **Points**: 40 XP

## Overview

Implement cluster purity, an external validation metric that scores clustering quality against known ground-truth labels.

## Task

Implement `purity_score(cluster_labels, true_labels)`. For each distinct cluster id in `cluster_labels`, find the count of its most common true label among members, sum those counts across all clusters, and divide by the total number of points.

<PracticePlayground problemId="cluster-rec-prob-22" />
