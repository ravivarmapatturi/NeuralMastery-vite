---
title: "Practice: Compute LLM Generation Latency Metrics"
description: "Compute production LLM serving benchmarks: Time To First Token (TTFT), Inter-Token Latency (ITL), and throughput (Tokens Per Second)."
difficulty: "easy"
topic: "transformers-llms"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Compute LLM Generation Latency Metrics

**Track**: transformers-llms | **Points**: 40 XP

## Overview

Compute production LLM serving benchmarks: Time To First Token (TTFT), Inter-Token Latency (ITL), and throughput (Tokens Per Second).

## Task

Implement `evaluate_generation_latencies(timestamps)`. Given arrival timestamps in seconds starting with request dispatch, return `ttft_ms`, `mean_itl_ms`, and `tokens_per_second` rounded to 2 decimal places.

<PracticePlayground problemId="llm-internals-prob-12" />
