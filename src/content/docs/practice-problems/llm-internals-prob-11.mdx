---
title: "Practice: Calculate KV-Cache Memory Footprint"
description: "Calculate the exact VRAM footprint in bytes required by an LLM KV-cache during multi-turn autoregressive serving."
difficulty: "easy"
topic: "transformers-llms"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Calculate KV-Cache Memory Footprint

**Track**: transformers-llms | **Points**: 40 XP

## Overview

Calculate the exact VRAM footprint in bytes required by an LLM KV-cache during multi-turn autoregressive serving.

## Task

Implement `calculate_kv_cache_bytes(num_layers, num_kv_heads, head_dim, seq_len, batch_size, bytes_per_param=2)`. Return the total byte count accounting for both Key and Value tensors across all layers, heads, positions, and batch slots.

<PracticePlayground problemId="llm-internals-prob-11" />
