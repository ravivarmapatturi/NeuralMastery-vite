---
title: "Practice: Is Subsequence"
description: "Master Is Subsequence in the Arrays / Hashing / Two Pointers track of the 1,000+ problem Neural Mastery AI Engineering curriculum."
difficulty: "easy"
topic: "Arrays / Hashing / Two Pointers"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# #539. Is Subsequence

**Track**: Arrays / Hashing / Two Pointers | **Rank**: #539 of 1072 | **Points**: 40 XP

## Overview

Implement `is_subsequence(s, t)` to test whether string `s` is embedded within string `t` in order, using linear-time greedy matching.

## Task

Implement the function `is_subsequence(s, t)`: return `True` if `s` is a subsequence of `t`, and `False` otherwise.

<PracticePlayground problemId="arr-hash-prob-9" />
