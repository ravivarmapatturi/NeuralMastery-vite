---
title: "Practice: Two Sum"
description: "Master Two Sum in the Arrays / Hashing / Two Pointers track of the 1,000+ problem Neural Mastery AI Engineering curriculum."
difficulty: "easy"
topic: "Arrays / Hashing / Two Pointers"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# #531. Arrays / Hashing / Two Pointers Module 1: Problem 1

**Track**: Arrays / Hashing / Two Pointers | **Rank**: #531 of 1072 | **Points**: 40 XP

## Overview

Implement `two_sum(nums, target)`, the canonical hash-map problem: an O(n) single-pass solution replacing the naive O(n^2) check of every pair, by trading memory for time.

## Task

Implement the function `two_sum(nums, target)`: return the indices `[i, j]` (with `i < j`) of the two numbers in `nums` that add up to `target`. Exactly one valid pair exists per input, and you may not use the same element twice. Raise `ValueError` if no pair sums to target.

<PracticePlayground problemId="arr-hash-prob-1" />
