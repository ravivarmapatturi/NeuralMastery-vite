---
title: "Practice: Tool Discovery Capability Filtering"
description: "Implement tool-discovery filtering by capability tag, letting a client or orchestrator narrow a large tool catalog to only relevant tools."
difficulty: "easy"
topic: "MCP Core Engineering"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Tool Discovery Capability Filtering

**Track**: MCP Core Engineering | **Points**: 40 XP

## Overview

Implement tool-discovery filtering by capability tag, letting a client or orchestrator narrow a large tool catalog to only relevant tools.

## Task

Implement `filter_tools_by_tag(tools, required_tag)`. `tools` is a list of `{"name": str, "tags": list[str]}`. Return the sorted list of tool names whose `tags` list contains `required_tag`.

<PracticePlayground problemId="mcp-core-prob-3" />
