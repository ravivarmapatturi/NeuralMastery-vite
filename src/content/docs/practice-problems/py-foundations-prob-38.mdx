---
title: "Practice: Circular Ring Buffer Simulator"
sidebar_position: 38
description: "Simulate a fixed-size ring buffer with automatic oldest-element eviction on overflow."
difficulty: medium
topic: python-fundamentals
---

import PracticePlayground from '../../../components/content/PracticePlayground';
import { Solution } from '../../../components/content/ExpandableDepth';

# Circular Ring Buffer Simulator

**Rank #38** · Difficulty: **Medium** · Stage: **Stage 1: AI Foundations & Python Programming** · Category: **Python & Fundamentals**

## Overview

Simulate a fixed-size ring buffer with automatic oldest-element eviction on overflow.

## Task

Implement `simulate_ring_buffer(operations, capacity)`: manage a circular buffer of fixed `capacity`. Operations are `push` (appends val, overwriting oldest if full), `pop` (removes and returns oldest item, or `None` if empty), and `to_list` (returns current items ordered oldest to newest). Return results for all `pop` and `to_list` operations.

<PracticePlayground problemId="py-foundations-prob-38" />

<Solution>

```python
from collections import deque

def simulate_ring_buffer(operations, capacity):
    buf = deque(maxlen=capacity)
    out = []
    for op in operations:
        action = op.get('op')
        if action == 'push':
            buf.append(op.get('val'))
        elif action == 'pop':
            out.append(buf.popleft() if buf else None)
        elif action == 'to_list':
            out.append(list(buf))
    return out
```

Python collections.deque with maxlen manages bounded size in O(1) time per push/pop operation.

</Solution>
