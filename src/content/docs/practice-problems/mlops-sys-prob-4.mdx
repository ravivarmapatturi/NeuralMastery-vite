---
title: "Practice: Z-Score Feature Standardization"
description: "Implement z-score standardization using pre-computed training statistics -- the real, correct way to scale features at inference time (never recompute mean/std on the inference batch)."
difficulty: "easy"
topic: "MLOps & Data Systems"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Z-Score Feature Standardization

**Track**: MLOps & Data Systems | **Points**: 40 XP

## Overview

Implement z-score standardization using pre-computed training statistics -- the real, correct way to scale features at inference time (never recompute mean/std on the inference batch).

## Task

Implement `standardize(values, mean, std)`: return `[(v - mean) / std for v in values]`.

<PracticePlayground problemId="mlops-sys-prob-4" />
