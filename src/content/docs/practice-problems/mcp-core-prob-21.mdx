---
title: "Practice: Message Size Limit Enforcement"
description: "Implement message-size limit enforcement, a real, simple defense against a malformed or malicious oversized MCP message exhausting server memory."
difficulty: "easy"
topic: "MCP Core Engineering"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Message Size Limit Enforcement

**Track**: MCP Core Engineering | **Points**: 40 XP

## Overview

Implement message-size limit enforcement, a real, simple defense against a malformed or malicious oversized MCP message exhausting server memory.

## Task

Implement `exceeds_size_limit(message_bytes, max_bytes)`: return `True` if `message_bytes > max_bytes`.

<PracticePlayground problemId="mcp-core-prob-21" />
