---
title: "Practice: Recursive Nested List Depth"
sidebar_position: 19
description: "Measure the maximum structural nesting depth of arbitrary recursive list hierarchies."
difficulty: easy
topic: python-fundamentals
---

import PracticePlayground from '../../../components/content/PracticePlayground';
import { Solution } from '../../../components/content/ExpandableDepth';

# Recursive Nested List Depth

**Rank #19** · Difficulty: **Easy** · Stage: **Stage 1: AI Foundations & Python Programming** · Category: **Python & Fundamentals**

## Overview

Measure the maximum structural nesting depth of arbitrary recursive list hierarchies.

## Task

Implement `list_depth(nested)`: return `0` if `nested` is not a list. If `nested` is a list, its depth is `1 + max(list_depth(elem) for elem in nested)` (or `1` if empty).

<PracticePlayground problemId="py-foundations-prob-19" />

<Solution>

```python
def list_depth(nested):
    if not isinstance(nested, list):
        return 0
    if not nested:
        return 1
    return 1 + max(list_depth(item) for item in nested)
```

Recursively inspecting each element and taking 1 + maximum sub-depth computes tree depth in O(N) where N is the total count of elements and sub-lists.

</Solution>
