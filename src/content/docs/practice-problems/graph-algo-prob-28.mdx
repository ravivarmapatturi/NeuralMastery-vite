---
title: "Practice: #648. Graphs & Network Algorithms Module 6: Problem 28"
description: "Master #648. Graphs & Network Algorithms Module 6: Problem 28 in the Graphs & Network Algorithms track of the 1,000+ problem Neural Mastery AI Engineering curriculum."
difficulty: "hard"
topic: "Graphs & Network Algorithms"
---

import PracticePlayground from '../../../components/content/PracticePlayground';
import { Solution } from '../../../components/content/ExpandableDepth';

# #648. Graphs & Network Algorithms Module 6: Problem 28

**Track**: Graphs & Network Algorithms | **Rank**: #648 of 1072 | **Points**

## Overview

Reconstruct all minimal word ladder transformation paths using level-by-level BFS and DFS backtracking.

## Task

Implement `find_all_ladders(beginWord, endWord, wordList)`: perform level-order BFS to construct predecessor DAG links at the shortest depth, then backtrack all minimal paths.

<PracticePlayground problemId="graph-algo-prob-28" />

<Solution>

```python
from collections import defaultdict

def find_all_ladders(beginWord, endWord, wordList):
    word_set = set(wordList)
    if endWord not in word_set:
        return []
    adj = defaultdict(list)
    layer = {beginWord}
    found = False
    while layer and not found:
        word_set -= layer
        next_layer = set()
        for w in layer:
            for i in range(len(w)):
                for c in 'abcdefghijklmnopqrstuvwxyz':
                    next_w = w[:i] + c + w[i+1:]
                    if next_w in word_set:
                        adj[w].append(next_w)
                        next_layer.add(next_w)
                        if next_w == endWord:
                            found = True
        layer = next_layer
    res = []
    def backtrack(curr, path):
        if curr == endWord:
            res.append(list(path))
            return
        for nxt in adj[curr]:
            path.append(nxt)
            backtrack(nxt, path)
            path.pop()
    backtrack(beginWord, [beginWord])
    return sorted(res)
```

Use BFS layer-by-layer to build a shortest-path DAG, pruning words only after completing each full level, followed by DFS to enumerate all minimal paths.

</Solution>
