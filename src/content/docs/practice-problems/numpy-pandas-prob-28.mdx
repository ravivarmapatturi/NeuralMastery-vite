---
title: "Practice: NumPy Module 28: Problem 208"
sidebar_position: 208
description: "Curriculum Problem 208 (Stage 4: NumPy & Vectorized Data Processing): Master NumPy / vectorization."
difficulty: medium
topic: numpy-pandas
---

import PracticePlayground from '../../../components/content/PracticePlayground';
import { Solution } from '../../../components/content/ExpandableDepth';

# NumPy Module 28: Problem 208

**Rank #208** · Difficulty: **Medium** · Stage: **Stage 4: NumPy & Vectorized Data Processing** · Category: **NumPy & Pandas**

## Overview

Compute an outer difference matrix using broadcasting principles.

## Task

Implement `broadcast_outer_difference(a, b)`: return an N x M matrix `M` where `M[i][j] = a[i] - b[j]`. Round each value to 4 decimal places.

<PracticePlayground problemId="numpy-pandas-prob-28" />

<Solution>

```python
def broadcast_outer_difference(a, b):
    return [[round(float(x - y), 4) for y in b] for x in a]
```

Broadcasting vector a as a column vector and b as a row vector evaluates all pairwise element subtractions simultaneously.

</Solution>
