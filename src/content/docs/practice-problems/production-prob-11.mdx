---
title: "Practice: Connection Pool Saturation Check"
description: "Implement connection-pool saturation detection, a real early-warning signal before a production agent's outbound HTTP client pool fully exhausts and starts queueing or rejecting new calls."
difficulty: "easy"
topic: "Agent Performance & Production"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Connection Pool Saturation Check

**Track**: Agent Performance & Production | **Points**: 40 XP

## Overview

Implement connection-pool saturation detection, a real early-warning signal before a production agent's outbound HTTP client pool fully exhausts and starts queueing or rejecting new calls.

## Task

Implement `is_pool_saturated(active_connections, pool_size, saturation_threshold_pct)`: return `True` if `(active_connections / pool_size * 100) >= saturation_threshold_pct`.

<PracticePlayground problemId="production-prob-11" />
