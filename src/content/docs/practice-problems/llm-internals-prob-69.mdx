---
title: "Practice: Compute Effective Batch Size With Gradient Accumulation"
description: "Implement effective batch size computation, the real arithmetic behind how gradient accumulation and multi-device training combine to reach a target training batch size that wouldn't otherwise fit in memory."
difficulty: "easy"
topic: "Transformers & LLMs"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Compute Effective Batch Size With Gradient Accumulation

**Track**: Transformers & LLMs | **Points**: 40 XP

## Overview

Implement effective batch size computation, the real arithmetic behind how gradient accumulation and multi-device training combine to reach a target training batch size that wouldn't otherwise fit in memory.

## Task

Implement `effective_batch_size(per_device_batch_size, num_devices, gradient_accumulation_steps)`: return `per_device_batch_size * num_devices * gradient_accumulation_steps`.

<PracticePlayground problemId="llm-internals-prob-69" />
