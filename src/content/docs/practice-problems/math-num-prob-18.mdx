---
title: "Practice: Bayes' Rule Categorical Posterior"
description: "Master #728. Mathematics & Numerical Computing Module 4: Problem 18 in the Mathematics & Numerical Computing track of the 1,000+ problem Neural Mastery AI Engineering curriculum."
difficulty: "medium"
topic: "Mathematics & Numerical Computing"
---

import PracticePlayground from '../../../components/content/PracticePlayground';
import { Solution } from '../../../components/content/ExpandableDepth';

# #728. Bayes' Rule Categorical Posterior

**Track**: Mathematics & Numerical Computing | **Rank**: #728 of 1072 | **Points**: 60 XP

## Overview

Compute the exact posterior distribution over discrete hypotheses using Bayes' rule.

## Task

Implement `bayes_posterior(priors, likelihoods)`: given prior probabilities `P(H_k)` and likelihoods of evidence `P(E | H_k)`, compute unnormalized products `u_k = priors[k] * likelihoods[k]`. Normalize by `sum(u)` so that posteriors sum to 1. Return list of posterior probabilities rounded to 4 decimals.

<PracticePlayground problemId="math-num-prob-18" />

<Solution>

```python
def bayes_posterior(priors, likelihoods):
    unnorm = [p * l for p, l in zip(priors, likelihoods)]
    total = sum(unnorm)
    if total < 1e-12:
        return [1.0 / len(priors)] * len(priors)
    post = [round(float(u / total), 4) for u in unnorm]
    return post
```

Calculates unnormalized joint probabilities and divides by the marginal evidence sum.

</Solution>
