---
title: "Practice: Validate a Knowledge Triple Shape"
description: "Implement knowledge-triple validation, the real structural gate before an extracted (subject, predicate, object) fact is inserted into a graph store."
difficulty: "easy"
topic: "Knowledge Graph & GraphRAG"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Validate a Knowledge Triple Shape

**Track**: Knowledge Graph & GraphRAG | **Points**: 40 XP

## Overview

Implement knowledge-triple validation, the real structural gate before an extracted (subject, predicate, object) fact is inserted into a graph store.

## Task

Implement `is_valid_triple(triple)`. Return `True` only if `triple` has exactly the keys `subject`, `predicate`, `object`, all three are non-empty strings, and `subject != object` (a self-loop fact is invalid).

<PracticePlayground problemId="graphrag-prob-2" />
