---
title: "Practice: Agent Recursive Delegation Depth Limit"
description: "Implement recursive delegation depth limiting, a real safety bound preventing an agent-calling-agent chain from recursing indefinitely."
difficulty: "easy"
topic: "MCP Advanced & Agent Integration"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Agent Recursive Delegation Depth Limit

**Track**: MCP Advanced & Agent Integration | **Points**: 40 XP

## Overview

Implement recursive delegation depth limiting, a real safety bound preventing an agent-calling-agent chain from recursing indefinitely.

## Task

Implement `can_delegate(current_depth, max_depth)`: return `True` if `current_depth < max_depth`.

<PracticePlayground problemId="mcp-adv-prob-10" />
