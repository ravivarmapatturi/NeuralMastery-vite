---
title: "Practice: Sort Colors (Dutch National Flag)"
description: "Master #546. Arrays / Hashing / Two Pointers Module 4: Problem 16 in the Arrays / Hashing / Two Pointers track of the 1,000+ problem Neural Mastery AI Engineering curriculum."
difficulty: "medium"
topic: "Arrays / Hashing / Two Pointers"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# #546. Arrays / Hashing / Two Pointers Module 4: Problem 16

**Track**: Arrays / Hashing / Two Pointers | **Rank**: #546 of 1072 | **Points**: 60 XP

## Overview

Implement `sort_colors(nums)`, Dijkstra's Dutch National Flag partition -- a single O(n) pass with three pointers that sorts a 3-valued array without calling a general sort.

## Task

Implement the function `sort_colors(nums)`: `nums` contains only the values `0`, `1`, and `2`. Return them sorted (all 0s, then all 1s, then all 2s) using the three-pointer Dutch National Flag partition.

<PracticePlayground problemId="arr-hash-prob-16" />
