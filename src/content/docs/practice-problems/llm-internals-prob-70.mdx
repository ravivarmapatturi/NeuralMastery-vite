---
title: "Practice: Detect Numerical Overflow Risk in Mixed-Precision Training"
description: "Implement a real overflow-risk check for mixed-precision (fp16) training, catching activations that are approaching the representable range's limit before they actually overflow to Inf/NaN."
difficulty: "medium"
topic: "Transformers & LLMs"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Detect Numerical Overflow Risk in Mixed-Precision Training

**Track**: Transformers & LLMs | **Points**: 40 XP

## Overview

Implement a real overflow-risk check for mixed-precision (fp16) training, catching activations that are approaching the representable range's limit before they actually overflow to Inf/NaN.

## Task

Implement `overflow_risk(activation_max_abs, dtype_max, safety_margin)`: return `True` if `activation_max_abs > dtype_max * (1 - safety_margin)` (within `safety_margin` fraction of the dtype's max representable value).

<PracticePlayground problemId="llm-internals-prob-70" />
