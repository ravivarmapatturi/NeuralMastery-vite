---
title: "Practice: Compute Real Precision and Recall for a Binary Judge"
description: "Implement precision and recall computation for a binary agent classification/judgment task, the standard real metrics distinguishing two different real error costs (false positives vs. false negatives)."
difficulty: "medium"
topic: "Agent Evaluation & Observability"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Compute Real Precision and Recall for a Binary Judge

**Track**: Agent Evaluation & Observability | **Points**: 40 XP

## Overview

Implement precision and recall computation for a binary agent classification/judgment task, the standard real metrics distinguishing two different real error costs (false positives vs. false negatives).

## Task

Implement `precision_recall(predictions, references)`. Both are lists of booleans (`True`=positive). `tp = count where prediction and reference both True`; `fp = prediction True, reference False`; `fn = prediction False, reference True`. `precision = tp/(tp+fp)` (0.0 if denominator is 0); `recall = tp/(tp+fn)` (0.0 if denominator is 0). Return `(precision, recall)`.

<PracticePlayground problemId="eval-obs-prob-23" />
