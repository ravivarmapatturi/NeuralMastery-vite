---
title: "Practice: Count Distinct Cited Sources"
description: "Implement distinct-source counting, a real signal for evaluating whether a RAG answer synthesized information from multiple sources or just one."
difficulty: "easy"
topic: "RAG Production Engineering"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Count Distinct Cited Sources

**Track**: RAG Production Engineering | **Points**: 40 XP

## Overview

Implement distinct-source counting, a real signal for evaluating whether a RAG answer synthesized information from multiple sources or just one.

## Task

Implement `count_distinct_sources(citations, chunk_to_source)`. `chunk_to_source` maps a chunk number to its source document name. Return the count of DISTINCT source documents among the cited chunk numbers (a citation not present in `chunk_to_source` is ignored).

<PracticePlayground problemId="rag-prod-prob-26" />
