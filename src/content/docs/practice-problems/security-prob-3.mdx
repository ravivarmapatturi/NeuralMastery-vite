---
title: "Practice: Tool Allowlist Enforcement"
description: "Implement tool-allowlist enforcement, a real, simple security boundary restricting an agent to only pre-approved tools rather than whatever it discovers."
difficulty: "easy"
topic: "Agent Security & Reliability"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Tool Allowlist Enforcement

**Track**: Agent Security & Reliability | **Points**: 40 XP

## Overview

Implement tool-allowlist enforcement, a real, simple security boundary restricting an agent to only pre-approved tools rather than whatever it discovers.

## Task

Implement `is_tool_allowed(tool_name, allowlist)`: return `True` if `tool_name` is in `allowlist`.

<PracticePlayground problemId="security-prob-3" />
