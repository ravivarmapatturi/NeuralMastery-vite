---
title: "Practice: #006. Transformer Internals Module 2: Problem 6"
description: "Master #006. Transformer Internals Module 2: Problem 6 in the Transformer Internals track of the 1,000+ problem Neural Mastery AI Engineering curriculum."
difficulty: "easy"
topic: "Transformer Internals"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# #006. Transformer Internals Module 2: Problem 6

**Track**: Transformer Internals | **Rank**: #6 of 1072 | **Points**: 40 XP

## Overview

Split an input hidden representation vector into multiple distinct attention heads. Multi-Head Attention allows the transformer to attend jointly to information from different representation subspaces at different positions.

## Task

Implement `split_heads(x, num_heads)`: divide a 1D vector `x` of dimension `d_model` into `num_heads` contiguous sub-vectors, each of dimension `d_head = len(x) // num_heads`. If `len(x) % num_heads != 0` or `num_heads <= 0` or `not x`, raise `ValueError`.

<PracticePlayground problemId="transformer-prob-6" />
