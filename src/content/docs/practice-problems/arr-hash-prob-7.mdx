---
title: "Practice: #537. Majority Element"
description: "Master #537. Majority Element in the Arrays / Hashing / Two Pointers track of the 1,000+ problem Neural Mastery AI Engineering curriculum."
difficulty: "easy"
topic: "Arrays / Hashing / Two Pointers"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# #537. Majority Element

**Track**: Arrays / Hashing / Two Pointers | **Rank**: #537 of 1072 | **Points**: 40 XP

## Overview

Implement `majority_element(nums)` to identify the dominant item that occurs more than half the time, applying Boyer-Moore's voting principle or hash-based frequency counting.

## Task

Implement the function `majority_element(nums)`: return the element in `nums` that appears more than `len(nums) // 2` times. Assume that the input array always contains a valid majority element.

<PracticePlayground problemId="arr-hash-prob-7" />
