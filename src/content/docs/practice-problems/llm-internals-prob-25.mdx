---
title: "Practice: Grouped Query Attention KV Head Count"
description: "Compute the number of key/value heads in Grouped Query Attention (GQA) given total query heads and groups."
difficulty: "easy"
topic: "transformers-llms"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Grouped Query Attention KV Head Count

**Track**: transformers-llms | **Points**: 40 XP

## Overview

Compute the number of key/value heads in Grouped Query Attention (GQA) given total query heads and groups.

## Task

Implement `gqa_kv_heads(num_q_heads, num_groups)` that returns `num_q_heads // num_groups` -- the number of key/value heads in Grouped Query Attention. Raise `ValueError` if either input is non-positive or if `num_q_heads % num_groups != 0`.

<PracticePlayground problemId="llm-internals-prob-25" />
