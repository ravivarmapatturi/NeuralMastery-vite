---
title: "Practice: Shortest Path Visiting All Nodes (Bitmask BFS)"
description: "Master Shortest Path Visiting All Nodes (Bitmask BFS) in the Graphs & Network Algorithms track of the 1,000+ problem Neural Mastery AI Engineering curriculum."
difficulty: "hard"
topic: "Graphs & Network Algorithms"
---

import PracticePlayground from '../../../components/content/PracticePlayground';
import { Solution } from '../../../components/content/ExpandableDepth';

# #645. Graphs & Network Algorithms Module 5: Problem 25

**Track**: Graphs & Network Algorithms | **Rank**: #645 of 1072 | **Points**

## Overview

Find the minimum steps to visit all nodes in an undirected graph using bitmask state-space BFS.

## Task

Implement `shortest_path_visiting_all_nodes(graph)`: maintain state `(curr_node, visited_mask)`. Seed BFS with all nodes and explore shortest path transitions until `mask == (1 << n) - 1`.

<PracticePlayground problemId="graph-algo-prob-25" />

<Solution>

```python
from collections import deque

def shortest_path_visiting_all_nodes(graph):
    n = len(graph)
    target_mask = (1 << n) - 1
    q = deque([(i, 1 << i, 0) for i in range(n)])
    visited = {(i, 1 << i) for i in range(n)}
    while q:
        node, mask, steps = q.popleft()
        if mask == target_mask:
            return steps
        for nbr in graph[node]:
            next_mask = mask | (1 << nbr)
            if (nbr, next_mask) not in visited:
                visited.add((nbr, next_mask))
                q.append((nbr, next_mask, steps + 1))
    return 0
```

Execute multi-source BFS over the compound state space (current_node, visited_bitmask). The first time any path reaches full mask coverage gives the minimal step count.

</Solution>
