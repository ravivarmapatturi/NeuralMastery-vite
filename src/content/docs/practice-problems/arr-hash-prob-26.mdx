---
title: "Practice: Sliding Window Maximum"
description: "Master Sliding Window Maximum in the Arrays / Hashing / Two Pointers track of the 1,000+ problem Neural Mastery AI Engineering curriculum."
difficulty: "hard"
topic: "Arrays / Hashing / Two Pointers"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# #556. Sliding Window Maximum

**Track**: Arrays / Hashing / Two Pointers | **Rank**: #556 of 1072 | **Points**: 90 XP

## Overview

Implement `max_sliding_window(nums, k)` to track the maximum element in an evolving window of size `k` in strictly linear time.

## Task

Implement the function `max_sliding_window(nums, k)`: return an array containing the maximum values for each window of length `k` as it slides across `nums`.

<PracticePlayground problemId="arr-hash-prob-26" />
