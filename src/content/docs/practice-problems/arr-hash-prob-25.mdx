---
title: "Practice: Minimum Window Substring"
description: "Master #555. Minimum Window Substring in the Arrays / Hashing / Two Pointers track of the 1,000+ problem Neural Mastery AI Engineering curriculum."
difficulty: "hard"
topic: "Arrays / Hashing / Two Pointers"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# #555. Minimum Window Substring

**Track**: Arrays / Hashing / Two Pointers | **Rank**: #555 of 1072 | **Points**: 90 XP

## Overview

Implement `min_window(s, t)` to discover the smallest contiguous window of `s` containing every character in `t` in linear time.

## Task

Implement the function `min_window(s, t)`: return the minimum window substring of `s` that covers all characters of `t`, or `""` if no such substring exists.

<PracticePlayground problemId="arr-hash-prob-25" />
