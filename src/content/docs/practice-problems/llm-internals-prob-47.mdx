---
title: "Practice: Reshape Flat Vector Into Multi-Head Attention Heads"
description: "Implement the multi-head split operation, dividing one flat query/key/value vector into the separate per-head sub-vectors multi-head attention actually operates on."
difficulty: "medium"
topic: "Transformers & LLMs"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Reshape Flat Vector Into Multi-Head Attention Heads

**Track**: Transformers & LLMs | **Points**: 40 XP

## Overview

Implement the multi-head split operation, dividing one flat query/key/value vector into the separate per-head sub-vectors multi-head attention actually operates on.

## Task

Implement `split_into_heads(x, num_heads)`. Split `x` into `num_heads` equal contiguous chunks. Return the list of chunks (each a list of floats).

<PracticePlayground problemId="llm-internals-prob-47" />
