---
title: "Practice: Filter Completion Suggestions by Prefix"
description: "Implement MCP argument-completion filtering, powering the real-time suggestion list a client shows while a user types a tool/prompt argument."
difficulty: "easy"
topic: "MCP Core Engineering"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Filter Completion Suggestions by Prefix

**Track**: MCP Core Engineering | **Points**: 40 XP

## Overview

Implement MCP argument-completion filtering, powering the real-time suggestion list a client shows while a user types a tool/prompt argument.

## Task

Implement `filter_completions(candidates, prefix)`: return the sorted list of `candidates` that start with `prefix` (case-insensitive match).

<PracticePlayground problemId="mcp-core-prob-27" />
