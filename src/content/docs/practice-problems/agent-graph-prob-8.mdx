---
title: "Practice: Find All Terminal Nodes"
description: "Implement terminal-node detection, finding every real exit point of an agent graph (nodes with no outgoing edges, where execution naturally ends)."
difficulty: "easy"
topic: "Agent Graph Engineering"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Find All Terminal Nodes

**Track**: Agent Graph Engineering | **Points**: 40 XP

## Overview

Implement terminal-node detection, finding every real exit point of an agent graph (nodes with no outgoing edges, where execution naturally ends).

## Task

Implement `find_terminal_nodes(edges)`: return the sorted list of node names whose outgoing edge list is empty.

<PracticePlayground problemId="agent-graph-prob-8" />
