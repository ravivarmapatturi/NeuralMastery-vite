---
title: "Practice: Exponential Backoff With Capped Jitter"
description: "Implement exponential backoff with a jitter RANGE (not a random draw), the real retry pattern any pipeline calling an external service (a model API, a data source) needs."
difficulty: "medium"
topic: "MLOps & Data Systems"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Exponential Backoff With Capped Jitter

**Track**: MLOps & Data Systems | **Points**: 40 XP

## Overview

Implement exponential backoff with a jitter RANGE (not a random draw), the real retry pattern any pipeline calling an external service (a model API, a data source) needs.

## Task

Implement `backoff_delay(attempt, base_delay_s, max_delay_s, jitter_fraction)`. Compute `raw = min(base_delay_s * (2 ** attempt), max_delay_s)`. Jitter range is `raw * jitter_fraction`. Return the tuple `(raw - jitter_range, raw + jitter_range)` -- the real caller draws a random delay from this range; we return the deterministic bounds so the result is testable.

<PracticePlayground problemId="mlops-sys-prob-27" />
