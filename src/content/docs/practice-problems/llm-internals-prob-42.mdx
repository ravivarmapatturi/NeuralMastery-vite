---
title: "Practice: Byte-Pair Encoding: One Merge Step"
description: "Implement one real Byte-Pair Encoding merge step, the core iterative operation that builds a BPE tokenizer's vocabulary from raw characters upward."
difficulty: "hard"
topic: "Transformers & LLMs"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Byte-Pair Encoding: One Merge Step

**Track**: Transformers & LLMs | **Points**: 40 XP

## Overview

Implement one real Byte-Pair Encoding merge step, the core iterative operation that builds a BPE tokenizer's vocabulary from raw characters upward.

## Task

Implement `bpe_merge_step(tokens, pair_to_merge)`. `pair_to_merge` is `(a, b)`. Scan `tokens` left to right; whenever two CONSECUTIVE tokens exactly equal `(a, b)`, replace them with the single merged token `a+b` (don't re-scan the merged result in the same pass -- move past it). Return the resulting token list.

<PracticePlayground problemId="llm-internals-prob-42" />
