---
title: "Practice: One-Hot Feature Encoding"
sidebar_position: 255
description: "Curriculum Problem 255 (Stage 5: Classical Machine Learning & Optimization): Master One-Hot Feature Encoding."
difficulty: easy
topic: classical-ml
---

import PracticePlayground from '../../../components/content/PracticePlayground';
import { Solution } from '../../../components/content/ExpandableDepth';

# One-Hot Feature Encoding

**Rank #255** · Difficulty: **Easy** · Stage: **Stage 5: Classical Machine Learning & Optimization** · Category: **Classical ML** | **Points**: 40 XP

## Overview

Implement `one_hot_encode(categories, vocabulary, handle_unknown="ignore")`, converting nominal categories into orthogonal indicator vectors for classical ML classifiers and regressors.

## Task

Implement `one_hot_encode(categories, vocabulary, handle_unknown="ignore")`: map categorical labels to binary vectors matching the order in `vocabulary`. Handle unknown labels gracefully according to `handle_unknown`.

<PracticePlayground problemId="classical-ml-prob-15" />

<Solution>

```python
def one_hot_encode(categories, vocabulary, handle_unknown="ignore"):
    vocab_map = {cat: i for i, cat in enumerate(vocabulary)}
    v_len = len(vocabulary)
    res = []
    for cat in categories:
        if cat in vocab_map:
            vec = [0] * v_len
            vec[vocab_map[cat]] = 1
            res.append(vec)
        else:
            if handle_unknown == "error":
                raise ValueError(f"Unknown category: {cat}")
            elif handle_unknown == "ignore":
                res.append([0] * v_len)
            else:
                raise ValueError(f"Unsupported handle_unknown mode: {handle_unknown}")
    return res
```

One-hot encoding removes false ordinal assumptions from categorical variables, creating orthogonal feature spaces for linear models and trees.

</Solution>
