---
title: "Practice: Data Exfiltration Egress URL Check"
description: "Implement egress-domain allowlisting, a real, critical safeguard preventing an agent's HTTP tool from sending data to an attacker-controlled external URL (a real exfiltration vector via prompt injection)."
difficulty: "medium"
topic: "Agent Security & Reliability"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Data Exfiltration Egress URL Check

**Track**: Agent Security & Reliability | **Points**: 40 XP

## Overview

Implement egress-domain allowlisting, a real, critical safeguard preventing an agent's HTTP tool from sending data to an attacker-controlled external URL (a real exfiltration vector via prompt injection).

## Task

Implement `is_egress_allowed(target_url, allowed_domains)`. Extract the domain: strip a leading `'http://'` or `'https://'`, then take everything up to (not including) the first `'/'` or end of string. Return `True` if that domain EXACTLY matches one in `allowed_domains` (no subdomain wildcarding).

<PracticePlayground problemId="security-prob-19" />
