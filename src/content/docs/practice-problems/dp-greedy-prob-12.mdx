---
title: "Practice: Minimum Path Sum in Grid"
description: "Master Minimum Path Sum in Grid in the Dynamic Programming & Greedy track of the 1,000+ problem Neural Mastery AI Engineering curriculum."
difficulty: "medium"
topic: "Dynamic Programming & Greedy"
---

import PracticePlayground from '../../../components/content/PracticePlayground';
import { Solution } from '../../../components/content/ExpandableDepth';

# #662. Dynamic Programming & Greedy Module 3: Problem 12

**Track**: Dynamic Programming & Greedy | **Rank**: #662 of 1072 | **Points**

## Overview

Find the path with minimal sum through a 2D matrix moving only right and down using dynamic programming.

## Task

Implement `min_path_sum(grid)`: dp[i][j] = grid[i][j] + min(dp[i-1][j], dp[i][j-1]). Compress to a 1D array of length n for O(n) space.

<PracticePlayground problemId="dp-greedy-prob-12" />

<Solution>

```python
def min_path_sum(grid):
    m, n = len(grid), len(grid[0])
    dp = [0] * n
    dp[0] = grid[0][0]
    for j in range(1, n):
        dp[j] = dp[j - 1] + grid[0][j]
    for i in range(1, m):
        dp[0] += grid[i][0]
        for j in range(1, n):
            dp[j] = min(dp[j], dp[j - 1]) + grid[i][j]
    return dp[-1]
```

Compute min path cost for each cell as the current cell value plus the min cost of arriving from above or from the left.

</Solution>
