---
title: "Practice: Prune Irrelevant Sentences From a Retrieved Chunk"
description: "Implement sentence-level context pruning, a real technique for trimming a retrieved chunk down to only its query-relevant sentences before it consumes generation context budget."
difficulty: "medium"
topic: "Advanced RAG & Agentic RAG"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Prune Irrelevant Sentences From a Retrieved Chunk

**Track**: Advanced RAG & Agentic RAG | **Points**: 40 XP

## Overview

Implement sentence-level context pruning, a real technique for trimming a retrieved chunk down to only its query-relevant sentences before it consumes generation context budget.

## Task

Implement `prune_chunk_sentences(query_words, sentences, min_overlap)`. For each sentence, count how many of its lowercase words are in `query_words`. Keep sentences with count `>= min_overlap`, preserving original order. If NO sentence meets the bar, return the original FULL sentence list unchanged (never prune everything away).

<PracticePlayground problemId="agentic-rag-prob-11" />
