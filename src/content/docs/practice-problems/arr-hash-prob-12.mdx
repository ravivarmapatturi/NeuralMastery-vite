---
title: "Practice: Product of Array Except Self"
description: "Master #542. Arrays / Hashing / Two Pointers Module 3: Problem 12 in the Arrays / Hashing / Two Pointers track of the 1,000+ problem Neural Mastery AI Engineering curriculum."
difficulty: "medium"
topic: "Arrays / Hashing / Two Pointers"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# #542. Arrays / Hashing / Two Pointers Module 3: Problem 12

**Track**: Arrays / Hashing / Two Pointers | **Rank**: #542 of 1072 | **Points**: 60 XP

## Overview

Implement `product_except_self(nums)` via two passes (prefix products, then suffix products) -- the classic no-division answer that also correctly handles zeros in the input.

## Task

Implement the function `product_except_self(nums)`: return a list where `result[i]` is the product of every element in `nums` except `nums[i]`. Must not use division, and must run in O(n) time.

<PracticePlayground problemId="arr-hash-prob-12" />
