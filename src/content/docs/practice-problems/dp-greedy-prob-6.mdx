---
title: "Practice: Counting Bits via DP"
description: "Master Counting Bits via DP in the Dynamic Programming & Greedy track of the 1,000+ problem Neural Mastery AI Engineering curriculum."
difficulty: "easy"
topic: "Dynamic Programming & Greedy"
---

import PracticePlayground from '../../../components/content/PracticePlayground';
import { Solution } from '../../../components/content/ExpandableDepth';

# #656. Dynamic Programming & Greedy Module 2: Problem 6

**Track**: Dynamic Programming & Greedy | **Rank**: #656 of 1072 | **Points**

## Overview

Count the number of set bits for all integers from 0 to n in O(n) linear time using bitwise dynamic programming.

## Task

Implement `count_bits(n)`: use the recurrence `dp[i] = dp[i >> 1] + (i & 1)` to determine the bit count of each integer from previously computed subproblems.

<PracticePlayground problemId="dp-greedy-prob-6" />

<Solution>

```python
def count_bits(n):
    ans = [0] * (n + 1)
    for i in range(1, n + 1):
        ans[i] = ans[i >> 1] + (i & 1)
    return ans
```

Shifting i right by 1 bit yields an already-computed smaller integer i // 2. The bit count of i is simply `ans[i >> 1]` plus 1 if the lowest bit is set.

</Solution>
