---
title: "Practice: #766. NumPy & Vectorized Computation Module 6: Problem 26"
description: "Master #766. NumPy & Vectorized Computation Module 6: Problem 26 in the NumPy & Vectorized Computation track of the 1,000+ problem Neural Mastery AI Engineering curriculum."
difficulty: "hard"
topic: "NumPy & Vectorized Computation"
---

import PracticePlayground from '../../../components/content/PracticePlayground';
import { Solution } from '../../../components/content/ExpandableDepth';

# #766. Vectorized Low-Rank SVD Reconstruction

**Track**: NumPy & Vectorized Computation | **Rank**: #766 of 1072 | **Points**: 90 XP

## Overview

Compress and reconstruct a matrix using Eckart-Young optimal rank-k truncated SVD.

## Task

Implement `low_rank_svd_reconstruct(U, S, Vt, rank_k)`: reconstruct matrix `A_k = sum_{i=0}^{rank_k - 1} S[i] * (U[:, i] (x) Vt[i, :])`. Return the `(M, N)` reconstructed matrix with all entries rounded to 4 decimals.

<PracticePlayground problemId="numpy-vec-prob-26" />

<Solution>

```python
def low_rank_svd_reconstruct(U, S, Vt, rank_k):
    M, N = len(U), len(Vt[0])
    res = [[0.0 for _ in range(N)] for _ in range(M)]
    for idx in range(rank_k):
        s = S[idx]
        for i in range(M):
            u = U[i][idx]
            for j in range(N):
                res[i][j] += s * u * Vt[idx][j]
    return [[round(float(v), 4) for v in row] for row in res]
```

Accumulates the first rank_k outer product rank-1 matrices scaled by their singular values.

</Solution>
