---
title: "Practice: Jump Game II (Minimum Jumps)"
description: "Master #666. Dynamic Programming & Greedy Module 4: Problem 16 in the Dynamic Programming & Greedy track of the 1,000+ problem Neural Mastery AI Engineering curriculum."
difficulty: "medium"
topic: "Dynamic Programming & Greedy"
---

import PracticePlayground from '../../../components/content/PracticePlayground';
import { Solution } from '../../../components/content/ExpandableDepth';

# #666. Dynamic Programming & Greedy Module 4: Problem 16

**Track**: Dynamic Programming & Greedy | **Rank**: #666 of 1072 | **Points**

## Overview

Find the minimum number of jumps required to reach the end of an array using BFS-level interval expansion.

## Task

Implement `min_jumps(nums)`: track the current jump horizon curr_end and the farthest reachable index farthest. When index i reaches curr_end, increment jumps and advance curr_end = farthest.

<PracticePlayground problemId="dp-greedy-prob-16" />

<Solution>

```python
def min_jumps(nums):
    if len(nums) <= 1:
        return 0
    jumps = 0
    curr_end = 0
    farthest = 0
    for i in range(len(nums) - 1):
        farthest = max(farthest, i + nums[i])
        if i == curr_end:
            jumps += 1
            curr_end = farthest
            if curr_end >= len(nums) - 1:
                break
    return jumps
```

Greedily expand the reachable boundary. Each increment of jumps corresponds to exhausting the reach of the previous jump tier and locking in the best reachable point.

</Solution>
