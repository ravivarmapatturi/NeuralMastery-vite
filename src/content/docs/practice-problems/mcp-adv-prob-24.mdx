---
title: "Practice: Server Discovery Registry Lookup"
description: "Implement server-discovery registry lookup, resolving a logical MCP server name to its actual connection endpoint."
difficulty: "easy"
topic: "MCP Advanced & Agent Integration"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Server Discovery Registry Lookup

**Track**: MCP Advanced & Agent Integration | **Points**: 40 XP

## Overview

Implement server-discovery registry lookup, resolving a logical MCP server name to its actual connection endpoint.

## Task

Implement `lookup_server_endpoint(server_name, registry)`: return `registry[server_name]`, or raise `KeyError` if not registered.

<PracticePlayground problemId="mcp-adv-prob-24" />
