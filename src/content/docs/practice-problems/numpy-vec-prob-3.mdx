---
title: "Practice: Vectorized Top-K Selection with Argpartition"
description: "Master Vectorized Top-K Selection with Argpartition in the NumPy & Vectorized Computation track of the 1,000+ problem Neural Mastery AI Engineering curriculum."
difficulty: "easy"
topic: "NumPy & Vectorized Computation"
---

import PracticePlayground from '../../../components/content/PracticePlayground';
import { Solution } from '../../../components/content/ExpandableDepth';

# #743. Vectorized Top-K Selection with Argpartition

**Track**: NumPy & Vectorized Computation | **Rank**: #743 of 1072 | **Points**: 40 XP

## Overview

Extract the top-k highest scoring elements along with their original indices.

## Task

Implement `top_k_elements(arr, k)`: select the `k` largest items in `arr`. Break ties by preserving lower original index first. Return a dict `{"values": list[float], "indices": list[int]}` where values are sorted descending and rounded to 4 decimals.

<PracticePlayground problemId="numpy-vec-prob-3" />

<Solution>

```python
def top_k_elements(arr, k):
    indexed = [(val, idx) for idx, val in enumerate(arr)]
    indexed.sort(key=lambda x: (-x[0], x[1]))
    top = indexed[:k]
    return {'values': [round(float(x[0]), 4) for x in top], 'indices': [x[1] for x in top]}
```

Pair values with indices, sort by descending value and ascending index, and return top k.

</Solution>
