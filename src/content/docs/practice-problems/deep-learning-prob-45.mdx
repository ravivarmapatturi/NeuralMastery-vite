---
title: "Practice: Dynamic Time Warping (DTW) Distance"
description: "Compute optimal non-linear time alignment distance between two variable-speed 1D speech or sensor sequences."
difficulty: "medium"
topic: "Deep Learning & Vision/NLP"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Dynamic Time Warping (DTW) Distance

**Track**: Deep Learning & Vision/NLP | **Points**: 40 XP

## Overview

Compute optimal non-linear time alignment distance between two variable-speed 1D speech or sensor sequences.

## Task

Implement `dtw_distance(seq1, seq2)`. Given sequences of lengths `N` and `M`, fill DP matrix `cost[i][j] = abs(seq1[i-1] - seq2[j-1]) + min(cost[i-1][j], cost[i][j-1], cost[i-1][j-1])`. Base case `cost[0][0] = 0.0`, all other `cost[0][j] = cost[i][0] = inf`. Return `cost[N][M]` rounded to 4 decimal places.

<PracticePlayground problemId="deep-learning-prob-45" />
