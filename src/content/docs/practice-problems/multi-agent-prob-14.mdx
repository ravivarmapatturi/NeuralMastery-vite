---
title: "Practice: Task-Category Hierarchical Delegation"
description: "Implement hierarchical delegation lookup, the real routing logic a supervisor agent uses to hand a task off to the right specialist sub-agent."
difficulty: "easy"
topic: "Multi-Agent Systems"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Task-Category Hierarchical Delegation

**Track**: Multi-Agent Systems | **Points**: 40 XP

## Overview

Implement hierarchical delegation lookup, the real routing logic a supervisor agent uses to hand a task off to the right specialist sub-agent.

## Task

Implement `delegate_to_subagent(task_category, delegation_map, default_agent)`: return `delegation_map[task_category]` if `task_category` is a key, otherwise return `default_agent`.

<PracticePlayground problemId="multi-agent-prob-14" />
