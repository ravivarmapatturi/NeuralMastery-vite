---
title: "Practice: Embedding Cache Hit Ratio"
description: "Implement embedding-cache hit-ratio computation, a real, standard metric for evaluating how much a RAG pipeline is saving on repeated embedding-API costs."
difficulty: "easy"
topic: "RAG Production Engineering"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Embedding Cache Hit Ratio

**Track**: RAG Production Engineering | **Points**: 40 XP

## Overview

Implement embedding-cache hit-ratio computation, a real, standard metric for evaluating how much a RAG pipeline is saving on repeated embedding-API costs.

## Task

Implement `cache_hit_ratio(requests, cache_keys)`: return the fraction of `requests` that are present in `cache_keys`. Return `0.0` for an empty request list.

<PracticePlayground problemId="rag-prod-prob-8" />
