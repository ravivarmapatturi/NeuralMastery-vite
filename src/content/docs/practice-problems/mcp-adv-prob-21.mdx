---
title: "Practice: Decide If a Tool Result Needs Summarization"
description: "Implement a real, simple summarization trigger for tool results, deciding when a raw result is too large to keep verbatim in the agent's working memory."
difficulty: "easy"
topic: "MCP Advanced & Agent Integration"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Decide If a Tool Result Needs Summarization

**Track**: MCP Advanced & Agent Integration | **Points**: 40 XP

## Overview

Implement a real, simple summarization trigger for tool results, deciding when a raw result is too large to keep verbatim in the agent's working memory.

## Task

Implement `needs_summarization(result_char_count, summarize_threshold)`: return `True` if `result_char_count > summarize_threshold`.

<PracticePlayground problemId="mcp-adv-prob-21" />
