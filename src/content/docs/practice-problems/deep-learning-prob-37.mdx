---
title: "Practice: Vision Transformer Patch Extraction and Flattening"
description: "Decompose a multi-channel 2D image into flattened sequential patch vectors for Transformer processing."
difficulty: "medium"
topic: "Deep Learning & Vision/NLP"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Vision Transformer Patch Extraction and Flattening

**Track**: Deep Learning & Vision/NLP | **Points**: 40 XP

## Overview

Decompose a multi-channel 2D image into flattened sequential patch vectors for Transformer processing.

## Task

Implement `vit_patch_flatten(image, patch_size)`. `image` has shape `C x H x W`. Slice non-overlapping patches of size `patch_size x patch_size` in row-major order (top to bottom, left to right). For each patch, flatten in channel-first order: `[patch[c][r][c_col] for c in range(C) for r in range(patch_size) for c_col in range(patch_size)]`. Return 2D list of shape `num_patches x (C * patch_size * patch_size)`.

<PracticePlayground problemId="deep-learning-prob-37" />
