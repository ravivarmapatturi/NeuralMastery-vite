---
title: "Practice: Chinchilla Compute-Optimal Token Count"
description: "Implement the Chinchilla scaling-law's real compute-optimal token calculation, the finding that reshaped how the field allocates a fixed training compute budget between model size and data size."
difficulty: "medium"
topic: "Transformers & LLMs"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Chinchilla Compute-Optimal Token Count

**Track**: Transformers & LLMs | **Points**: 40 XP

## Overview

Implement the Chinchilla scaling-law's real compute-optimal token calculation, the finding that reshaped how the field allocates a fixed training compute budget between model size and data size.

## Task

Implement `chinchilla_optimal_tokens(num_params, tokens_per_param)`: return `num_params * tokens_per_param`.

<PracticePlayground problemId="llm-internals-prob-64" />
