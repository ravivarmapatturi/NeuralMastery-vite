---
title: "Practice: Course Schedule: Cycle Detection in Directed Graph"
description: "Master Course Schedule: Cycle Detection in Directed Graph in the Graphs & Network Algorithms track of the 1,000+ problem Neural Mastery AI Engineering curriculum."
difficulty: "medium"
topic: "Graphs & Network Algorithms"
---

import PracticePlayground from '../../../components/content/PracticePlayground';
import { Solution } from '../../../components/content/ExpandableDepth';

# #631. Graphs & Network Algorithms Module 3: Problem 11

**Track**: Graphs & Network Algorithms | **Rank**: #631 of 1072 | **Points**

## Overview

Determine whether a course dependency graph contains cycles using Kahn's BFS topological sort.

## Task

Implement `can_finish_courses(numCourses, prerequisites)`: track in-degrees. Enqueue all courses with in-degree 0. As each course is processed, decrement neighbors' in-degrees. Return True if all courses are visited.

<PracticePlayground problemId="graph-algo-prob-11" />

<Solution>

```python
from collections import deque, defaultdict

def can_finish_courses(numCourses, prerequisites):
    adj = defaultdict(list)
    in_degree = [0] * numCourses
    for dest, src in prerequisites:
        adj[src].append(dest)
        in_degree[dest] += 1
    q = deque([i for i in range(numCourses) if in_degree[i] == 0])
    visited_count = 0
    while q:
        curr = q.popleft()
        visited_count += 1
        for nbr in adj[curr]:
            in_degree[nbr] -= 1
            if in_degree[nbr] == 0:
                q.append(nbr)
    return visited_count == numCourses
```

Construct the dependency DAG and track in-degrees. Process nodes with zero in-degree iteratively; any unvisited node indicates participation in a directed cycle.

</Solution>
