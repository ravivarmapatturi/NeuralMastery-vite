---
title: "Practice: Autoscaling Target Replica Count"
description: "Implement real autoscaling target computation for an agent-serving deployment, the direct arithmetic behind a Kubernetes HPA-style scaling decision."
difficulty: "medium"
topic: "Agent Performance & Production"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Autoscaling Target Replica Count

**Track**: Agent Performance & Production | **Points**: 40 XP

## Overview

Implement real autoscaling target computation for an agent-serving deployment, the direct arithmetic behind a Kubernetes HPA-style scaling decision.

## Task

Implement `target_replicas(current_qps, qps_per_replica, min_replicas, max_replicas)`. Raw target is `ceil(current_qps / qps_per_replica)`. Clamp the result to `[min_replicas, max_replicas]`.

<PracticePlayground problemId="production-prob-3" />
