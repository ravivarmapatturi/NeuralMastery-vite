---
title: "Practice: Subarray Sum Equals K"
description: "Master Subarray Sum Equals K in the Arrays / Hashing / Two Pointers track of the 1,000+ problem Neural Mastery AI Engineering curriculum."
difficulty: "medium"
topic: "Arrays / Hashing / Two Pointers"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# #545. Arrays / Hashing / Two Pointers Module 3: Problem 15

**Track**: Arrays / Hashing / Two Pointers | **Rank**: #545 of 1072 | **Points**: 60 XP

## Overview

Implement `subarray_sum(nums, k)` via the prefix-sum-plus-hash-map trick -- the technique that makes this solvable in O(n) even with negative numbers present, where a sliding window would fail.

## Task

Implement the function `subarray_sum(nums, k)`: return the number of contiguous subarrays of `nums` whose elements sum to exactly `k`. `nums` may contain negative numbers.

<PracticePlayground problemId="arr-hash-prob-15" />
