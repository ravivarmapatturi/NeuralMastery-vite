---
title: "Practice: GPU Memory Fragmentation Estimate"
description: "Implement a real GPU-memory fragmentation estimate, explaining a genuinely confusing real production symptom: an out-of-memory error despite plenty of TOTAL free memory reported."
difficulty: "medium"
topic: "Agent Performance & Production"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# GPU Memory Fragmentation Estimate

**Track**: Agent Performance & Production | **Points**: 40 XP

## Overview

Implement a real GPU-memory fragmentation estimate, explaining a genuinely confusing real production symptom: an out-of-memory error despite plenty of TOTAL free memory reported.

## Task

Implement `estimate_fragmentation(total_free_mb, largest_free_block_mb)`. Return `1 - (largest_free_block_mb / total_free_mb)` -- the fraction of free memory that is NOT usable as one contiguous allocation. Return `0.0` if `total_free_mb` is 0.

<PracticePlayground problemId="production-prob-16" />
