---
title: "Practice: Run-Length String Compression"
sidebar_position: 17
description: "Encode repeated consecutive characters into character-count pairs, the cornerstone of lossless text and bitmap data compression."
difficulty: easy
topic: python-fundamentals
---

import PracticePlayground from '../../../components/content/PracticePlayground';
import { Solution } from '../../../components/content/ExpandableDepth';

# Run-Length String Compression

**Rank #17** · Difficulty: **Easy** · Stage: **Stage 1: AI Foundations & Python Programming** · Category: **Python & Fundamentals**

## Overview

Encode repeated consecutive characters into character-count pairs, the cornerstone of lossless text and bitmap data compression.

## Task

Implement `run_length_encode(text)`: walk through the string and replace each run of identical consecutive characters with the character followed by the count of repetitions. Return `""` for an empty string.

<PracticePlayground problemId="py-foundations-prob-17" />

<Solution>

```python
def run_length_encode(text):
    if not text:
        return ""
    res = []
    curr_char = text[0]
    count = 1
    for ch in text[1:]:
        if ch == curr_char:
            count += 1
        else:
            res.append(f"{curr_char}{count}")
            curr_char = ch
            count = 1
    res.append(f"{curr_char}{count}")
    return "".join(res)
```

Iterating through characters with an accumulator list and joining once at the end avoids Python quadratic string reallocation overhead.

</Solution>
