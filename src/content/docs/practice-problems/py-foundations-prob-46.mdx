---
title: "Practice: Levenshtein Edit Distance Metric"
sidebar_position: 46
description: "Measure string similarity using the classical Wagner-Fischer dynamic programming edit distance."
difficulty: medium
topic: python-fundamentals
---

import PracticePlayground from '../../../components/content/PracticePlayground';
import { Solution } from '../../../components/content/ExpandableDepth';

# Levenshtein Edit Distance Metric

**Rank #46** · Difficulty: **Medium** · Stage: **Stage 1: AI Foundations & Python Programming** · Category: **Python & Fundamentals**

## Overview

Measure string similarity using the classical Wagner-Fischer dynamic programming edit distance.

## Task

Implement `edit_distance(word1, word2)`: calculate the minimum number of operations (insert a character, delete a character, replace a character) needed to transform `word1` into `word2`.

<PracticePlayground problemId="py-foundations-prob-46" />

<Solution>

```python
def edit_distance(word1, word2):
    m, n = len(word1), len(word2)
    dp = [[0] * (n + 1) for _ in range(m + 1)]
    for i in range(m + 1):
        dp[i][0] = i
    for j in range(n + 1):
        dp[0][j] = j
    for i in range(1, m + 1):
        for j in range(1, n + 1):
            if word1[i - 1] == word2[j - 1]:
                dp[i][j] = dp[i - 1][j - 1]
            else:
                dp[i][j] = 1 + min(dp[i - 1][j], dp[i][j - 1], dp[i - 1][j - 1])
    return dp[m][n]
```

The Wagner-Fischer algorithm computes optimal sequence alignment transitions in O(m * n) polynomial time.

</Solution>
