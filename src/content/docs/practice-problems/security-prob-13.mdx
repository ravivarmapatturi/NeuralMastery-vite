---
title: "Practice: Sandbox Resource Limit Enforcement"
description: "Implement sandbox resource-limit enforcement, a real safety check before letting an agent's tool execution continue consuming shared compute."
difficulty: "easy"
topic: "Agent Security & Reliability"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Sandbox Resource Limit Enforcement

**Track**: Agent Security & Reliability | **Points**: 40 XP

## Overview

Implement sandbox resource-limit enforcement, a real safety check before letting an agent's tool execution continue consuming shared compute.

## Task

Implement `within_resource_limits(cpu_pct, memory_mb, max_cpu_pct, max_memory_mb)`: return `True` only if BOTH `cpu_pct <= max_cpu_pct` AND `memory_mb <= max_memory_mb`.

<PracticePlayground problemId="security-prob-13" />
