---
title: "Practice: Connected Components via Union-Find"
description: "Implement connected-components detection via union-find, a real, simple community-detection baseline for a knowledge graph."
difficulty: "hard"
topic: "Knowledge Graph & GraphRAG"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Connected Components via Union-Find

**Track**: Knowledge Graph & GraphRAG | **Points**: 40 XP

## Overview

Implement connected-components detection via union-find, a real, simple community-detection baseline for a knowledge graph.

## Task

Implement `connected_components(edges)`. Each edge is `[node_a, node_b]` (treat the graph as undirected for this purpose). Return a list of components, each a sorted list of node names, where each component is itself sorted, and the outer list is sorted by each component's first (smallest) node name.

<PracticePlayground problemId="graphrag-prob-9" />
