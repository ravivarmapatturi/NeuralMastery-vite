---
title: "Practice: 3Sum"
description: "Master #547. 3Sum in the Arrays / Hashing / Two Pointers track of the 1,000+ problem Neural Mastery AI Engineering curriculum."
difficulty: "medium"
topic: "Arrays / Hashing / Two Pointers"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# #547. 3Sum

**Track**: Arrays / Hashing / Two Pointers | **Rank**: #547 of 1072 | **Points**: 60 XP

## Overview

Implement `three_sum(nums)` to identify all unique zero-sum triplets in O(n^2) time via array sorting and duplicate-skipping two-pointer search.

## Task

Implement the function `three_sum(nums)`: return all unique triplets `[nums[i], nums[j], nums[k]]` such that `nums[i] + nums[j] + nums[k] == 0` without duplicate sets.

<PracticePlayground problemId="arr-hash-prob-17" />
