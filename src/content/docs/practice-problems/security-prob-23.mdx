---
title: "Practice: Cumulative Resource Quota Enforcement"
description: "Implement cumulative resource quota enforcement, a real spend/usage cap distinct from a per-request rate limit -- bounding TOTAL usage over a billing period rather than instantaneous rate."
difficulty: "easy"
topic: "Agent Security & Reliability"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Cumulative Resource Quota Enforcement

**Track**: Agent Security & Reliability | **Points**: 40 XP

## Overview

Implement cumulative resource quota enforcement, a real spend/usage cap distinct from a per-request rate limit -- bounding TOTAL usage over a billing period rather than instantaneous rate.

## Task

Implement `within_quota(used_so_far, additional_usage, quota)`: return `True` if `(used_so_far + additional_usage) <= quota`.

<PracticePlayground problemId="security-prob-23" />
