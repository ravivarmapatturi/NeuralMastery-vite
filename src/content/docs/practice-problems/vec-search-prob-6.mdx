---
title: "Practice: Brute-Force K-Nearest-Neighbor Search"
description: "Master #981. Vector Search & Index Optimization Module 2: Problem 6 in the Vector Search & Index Optimization track of the 1,000+ problem Neural Mastery AI Engineering curriculum."
difficulty: "easy"
topic: "Vector Search & Index Optimization"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# #981. Vector Search & Index Optimization Module 2: Problem 6

**Track**: Vector Search & Index Optimization | **Rank**: #981 of 1072 | **Points**: 40 XP

## Overview

Implement `brute_force_knn(query, vectors, k)`, the exact ('flat') k-nearest-neighbor search that every approximate nearest-neighbor (ANN) index -- HNSW, IVF, LSH -- is an approximation of. It's the ground-truth baseline used to measure the recall of every faster index structure.

## Task

Implement the function `brute_force_knn(query, vectors, k)`: compute the Euclidean distance from `query` to every row in `vectors`, and return the indices of the `k` nearest, ordered nearest-first with ties broken by ascending index. Raise `ValueError` if `k <= 0` or `k > len(vectors)`.

<PracticePlayground problemId="vec-search-prob-6" />
