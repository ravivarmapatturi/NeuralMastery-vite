---
title: "Practice: Self-Query: Extract Structured Metadata Filter From Natural Language"
description: "Implement self-querying metadata extraction, a real technique letting a RAG system parse structured filters (year, category, author) directly out of a natural-language query rather than requiring separate filter UI."
difficulty: "medium"
topic: "Advanced RAG & Agentic RAG"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Self-Query: Extract Structured Metadata Filter From Natural Language

**Track**: Advanced RAG & Agentic RAG | **Points**: 40 XP

## Overview

Implement self-querying metadata extraction, a real technique letting a RAG system parse structured filters (year, category, author) directly out of a natural-language query rather than requiring separate filter UI.

## Task

Implement `extract_metadata_filter(query, filter_keywords)`. `filter_keywords` maps a keyword to a `(field, value)`-shaped string like `'year:2023'`. For each keyword found (case-insensitive whole-word match) in `query`, add its field:value pair to the result dict. Return the accumulated filter dict.

<PracticePlayground problemId="agentic-rag-prob-8" />
