---
title: "Practice: #715. Mathematics & Numerical Computing Module 1: Problem 5"
description: "Master #715. Mathematics & Numerical Computing Module 1: Problem 5 in the Mathematics & Numerical Computing track of the 1,000+ problem Neural Mastery AI Engineering curriculum."
difficulty: "easy"
topic: "Mathematics & Numerical Computing"
---

import PracticePlayground from '../../../components/content/PracticePlayground';
import { Solution } from '../../../components/content/ExpandableDepth';

# #715. Gram-Schmidt Orthonormalization

**Track**: Mathematics & Numerical Computing | **Rank**: #715 of 1072 | **Points**: 40 XP

## Overview

Construct an orthonormal basis using the classical Gram-Schmidt orthogonalization process.

## Task

Implement `gram_schmidt(vectors)`: sequentially project each vector onto the span of previously computed basis vectors, subtract projections to get orthogonal vector w, normalize w to unit length, and append to basis. Discard vectors whose remaining norm is `<` 1e-7. Round coordinates to 4 decimals.

<PracticePlayground problemId="math-num-prob-5" />

<Solution>

```python
import math

def gram_schmidt(vectors):
    basis = []
    for v in vectors:
        w = list(v)
        for u in basis:
            dot_wu = sum(a * b for a, b in zip(w, u))
            dot_uu = sum(b * b for b in u)
            if dot_uu > 1e-9:
                scalar = dot_wu / dot_uu
                w = [a - scalar * b for a, b in zip(w, u)]
        norm_w = math.sqrt(sum(a ** 2 for a in w))
        if norm_w > 1e-7:
            u_norm = [a / norm_w for a in w]
            basis.append(u_norm)
    return [[round(float(x), 4) for x in u] for u in basis]
```

Iteratively subtracts projections against all existing orthonormal basis vectors, then normalizes.

</Solution>
