---
title: "Practice: Cosine Similarity Between Vectors"
description: "Master Cosine Similarity Between Vectors in the Vector Search & Index Optimization track of the 1,000+ problem Neural Mastery AI Engineering curriculum."
difficulty: "easy"
topic: "Vector Search & Index Optimization"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# #978. Vector Search & Index Optimization Module 1: Problem 3

**Track**: Vector Search & Index Optimization | **Rank**: #978 of 1072 | **Points**: 40 XP

## Overview

Implement `cosine_similarity(a, b)`, the default relevance metric behind embedding-based semantic search and RAG retrieval, because it measures directional alignment between two vectors while ignoring their magnitude.

## Task

Implement the function `cosine_similarity(a, b)`, returning `(a . b) / (||a|| * ||b||)` for two equal-length numeric vectors `a` and `b`. Raise `ValueError` if `len(a) != len(b)`, or if either vector has zero magnitude (its direction is undefined).

<PracticePlayground problemId="vec-search-prob-3" />
