---
title: "Practice: Infer MIME Type From Resource Extension"
description: "Implement MIME-type inference from a resource URI's file extension, needed so an MCP server can populate a resource's `mimeType` field correctly."
difficulty: "easy"
topic: "MCP Core Engineering"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Infer MIME Type From Resource Extension

**Track**: MCP Core Engineering | **Points**: 40 XP

## Overview

Implement MIME-type inference from a resource URI's file extension, needed so an MCP server can populate a resource's `mimeType` field correctly.

## Task

Implement `infer_mime_type(uri)`. Extract the extension (text after the last `'.'`, lowercase). Map: `txt->'text/plain'`, `json->'application/json'`, `md->'text/markdown'`, `py->'text/x-python'`, `html->'text/html'`, `csv->'text/csv'`. Any other or missing extension returns `'application/octet-stream'`.

<PracticePlayground problemId="mcp-core-prob-22" />
