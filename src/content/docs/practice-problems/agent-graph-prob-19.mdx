---
title: "Practice: Evaluate END-Condition on State"
description: "Implement the graph's real termination-condition check, deciding whether the current state means the agent workflow is genuinely done."
difficulty: "easy"
topic: "Agent Graph Engineering"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Evaluate END-Condition on State

**Track**: Agent Graph Engineering | **Points**: 40 XP

## Overview

Implement the graph's real termination-condition check, deciding whether the current state means the agent workflow is genuinely done.

## Task

Implement `should_end(state)`: return `True` if `state.get('status') == 'complete'` OR `state.get('error') is not None`.

<PracticePlayground problemId="agent-graph-prob-19" />
