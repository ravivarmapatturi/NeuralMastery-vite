---
title: "Practice: Spectral Clustering: Gaussian Affinity Matrix"
description: "Implement the Gaussian (RBF) affinity matrix, the first real step of spectral clustering -- turning raw points into a similarity graph."
difficulty: "medium"
topic: "Clustering & Recommender Systems"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# Spectral Clustering: Gaussian Affinity Matrix

**Track**: Clustering & Recommender Systems | **Points**: 40 XP

## Overview

Implement the Gaussian (RBF) affinity matrix, the first real step of spectral clustering -- turning raw points into a similarity graph.

## Task

Implement `gaussian_affinity_matrix(points, sigma)`. Return the `n x n` matrix where entry `[i][j] = exp(-euclidean(points[i], points[j])**2 / (2*sigma**2))`. Diagonal entries (i==i) should be `1.0` (distance 0 gives exp(0)=1 naturally -- just confirming the expected convention).

<PracticePlayground problemId="cluster-rec-prob-29" />
