---
title: "Practice: #875. Deep Learning & Neural Networks Module 3: Problem 15"
description: "Master #875. Deep Learning & Neural Networks Module 3: Problem 15 in the Deep Learning & Neural Networks track of the 1,000+ problem Neural Mastery AI Engineering curriculum."
difficulty: "medium"
topic: "Deep Learning & Neural Networks"
---

import PracticePlayground from '../../../components/content/PracticePlayground';
import { Solution } from '../../../components/content/ExpandableDepth';

# #875. RMSprop Optimizer Parameter Step

**Track**: Deep Learning & Neural Networks | **Rank**: #875 of 1072 | **Points**: 60 XP

## Overview

Resolve AdaGrad's diminishing learning rates by keeping an exponentially decaying average of squared gradients.

## Task

Implement `rmsprop_step(param, grad, square_avg, lr=0.01, alpha=0.99, eps=1e-8)`: for each index `k`, update the squared gradient moving average: `v_k = alpha * square_avg[k] + (1 - alpha) * grad[k]^2`. Update the parameter: `param_k = param[k] - (lr / (sqrt(v_k) + eps)) * grad[k]`. Return `{'param': ..., 'square_avg': ...}` with values rounded to 6 decimal places.

<PracticePlayground problemId="dl-nn-prob-15" />

<Solution>

```python
def rmsprop_step(param: list[float], grad: list[float], square_avg: list[float], lr: float = 0.01, alpha: float = 0.99, eps: float = 1e-8) -> dict:
    new_p = []
    new_sq = []
    for p, g, sq in zip(param, grad, square_avg):
        sq_t = alpha * sq + (1.0 - alpha) * (g ** 2)
        p_t = p - (lr / (math.sqrt(sq_t) + eps)) * g
        new_sq.append(round(sq_t, 6))
        new_p.append(round(p_t, 6))
    return {"param": new_p, "square_avg": new_sq}
```

RMSprop divides the gradient by the square root of an exponential moving average of squared gradients, maintaining steady learning rates on non-convex surfaces.

</Solution>
