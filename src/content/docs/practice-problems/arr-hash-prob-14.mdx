---
title: "Practice: Container With Most Water"
description: "Master #544. Arrays / Hashing / Two Pointers Module 3: Problem 14 in the Arrays / Hashing / Two Pointers track of the 1,000+ problem Neural Mastery AI Engineering curriculum."
difficulty: "medium"
topic: "Arrays / Hashing / Two Pointers"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# #544. Arrays / Hashing / Two Pointers Module 3: Problem 14

**Track**: Arrays / Hashing / Two Pointers | **Rank**: #544 of 1072 | **Points**: 60 XP

## Overview

Implement `max_area(heights)`, the greedy two-pointer solution to Container With Most Water -- the classic proof that checking every pair is unnecessary once you see why moving the shorter pointer is always the only move worth making.

## Task

Implement the function `max_area(heights)`: `heights[i]` is the height of a vertical line at position `i`. Return the maximum area a pair of these lines (plus the x-axis between them) can contain.

<PracticePlayground problemId="arr-hash-prob-14" />
