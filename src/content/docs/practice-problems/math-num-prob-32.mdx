---
title: "Practice: Mathematics Module 32: Problem 152"
sidebar_position: 152
description: "Curriculum Problem 152 (Stage 3: Mathematics & Numerical Computing): Master Mathematics / numerical computing."
difficulty: medium
topic: math-numerical
---

import PracticePlayground from '../../../components/content/PracticePlayground';
import { Solution } from '../../../components/content/ExpandableDepth';

# Mathematics Module 32: Problem 152

**Rank #152** · Difficulty: **Medium** · Stage: **Stage 3: Mathematics & Numerical Computing** · Category: **Math & Numerical**

## Overview

Verify whether a square linear transform preserves vector lengths and angles.

## Task

Implement `is_orthogonal_matrix(A, tol=1e-5)`: check whether `A^T * A` equals the identity matrix `I` within entry-wise absolute tolerance `tol`. Return True if orthogonal, else False.

<PracticePlayground problemId="math-num-prob-32" />

<Solution>

```python
def is_orthogonal_matrix(A, tol=1e-5):
    n = len(A)
    for i in range(n):
        for j in range(n):
            dot = sum(A[k][i] * A[k][j] for k in range(n))
            expected = 1.0 if i == j else 0.0
            if abs(dot - expected) > tol:
                return False
    return True
```

Verifies that all columns form an orthonormal system via pairwise dot products.

</Solution>
