---
title: "Practice: Find All Anagrams in a String"
description: "Master #557. Find All Anagrams in a String in the Arrays / Hashing / Two Pointers track of the 1,000+ problem Neural Mastery AI Engineering curriculum."
difficulty: "hard"
topic: "Arrays / Hashing / Two Pointers"
---

import PracticePlayground from '../../../components/content/PracticePlayground';

# #557. Find All Anagrams in a String

**Track**: Arrays / Hashing / Two Pointers | **Rank**: #557 of 1072 | **Points**: 90 XP

## Overview

Implement `find_anagrams(s, p)` using a fixed-width sliding window to identify all starting positions where an anagram of `p` occurs within `s`.

## Task

Implement the function `find_anagrams(s, p)`: return all starting indices of substrings in `s` that form an anagram of `p`, sorted in ascending order.

<PracticePlayground problemId="arr-hash-prob-27" />
